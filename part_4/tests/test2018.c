//
// Created by nadav on 4/21/2019.
//

#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#include <assert.h>
#include "list.h"
#include "eurovision.h"
#include "eurovisionTests.h"

#define POINTS_OPTIONS_NUMBER 10

#define CHECK(b,res)                            \
  if((b) != (res)) do{                          \
      printf("fail: %s != %s\n", #b, #res);     \
      eurovisionDestroy(eurovision);            \
      return false;                             \
    } while(0)

#define CHECK_WITH_FREE(b,res,f)                \
  if((b) != (res)) do{                          \
      printf("fail: %s != %s\n", #b, #res);     \
      eurovisionDestroy(eurovision);            \
      free(f);                                  \
      return false;                             \
    } while(0)

static Eurovision setupEurovision() {
    Eurovision eurovision = eurovisionCreate();
    assert(eurovision);
    return eurovision;
}

static void setupEurovisionStates(Eurovision eurovision) {
    eurovisionAddState(eurovision, 1, "ukraine", "under the ladder");
    eurovisionAddState(eurovision, 2, "spain", "tu cancion");
    eurovisionAddState(eurovision, 3, "slovenia", "havala ne");
    eurovisionAddState(eurovision, 4, "lithuania", "when were old");
    eurovisionAddState(eurovision, 5, "austria", "nobody but you");
    eurovisionAddState(eurovision, 6, "estonia", "la forza");
    eurovisionAddState(eurovision, 7, "norway", "thats how you write a song");
    eurovisionAddState(eurovision, 8, "portugal", "o jardim");
    eurovisionAddState(eurovision, 9, "united kingdom", "storm");
    eurovisionAddState(eurovision, 10, "serbia", "nova deca");
    eurovisionAddState(eurovision, 11, "germany", "you let me walk alone");
    eurovisionAddState(eurovision, 12, "albania", "mall");
    eurovisionAddState(eurovision, 13, "france", "mercy");
    eurovisionAddState(eurovision, 14, "czech republic", "lie to me");
    eurovisionAddState(eurovision, 15, "denmark", "higer");
    eurovisionAddState(eurovision, 16, "australia", "we got love");
    eurovisionAddState(eurovision, 17, "finland", "monsters");
    eurovisionAddState(eurovision, 18, "bulgaria", "bones");
    eurovisionAddState(eurovision, 19, "moldova", "my lucky day");
    eurovisionAddState(eurovision, 20, "sweden", "dance you off");
    eurovisionAddState(eurovision, 21, "hungary", "viszlat nyar");
    eurovisionAddState(eurovision, 22, "israel", "toy");
    eurovisionAddState(eurovision, 23, "netherlands", "outlaw in em");
    eurovisionAddState(eurovision, 24, "ireland", "together");
    eurovisionAddState(eurovision, 25, "cyprus", "fuego");
    eurovisionAddState(eurovision, 26, "italy", "non mi avete fatto niente");
    eurovisionAddState(eurovision, 27, "azerbaijan", "out of contest");
    eurovisionAddState(eurovision, 28, "belarus", "out of contest");
    eurovisionAddState(eurovision, 29, "san marino", "out of contest");
    eurovisionAddState(eurovision, 30, "macedonia", "out of contest");
    eurovisionAddState(eurovision, 31, "malta", "out of contest");
    eurovisionAddState(eurovision, 32, "georgia", "out of contest");
    eurovisionAddState(eurovision, 33, "latvia", "out of contest");
    eurovisionAddState(eurovision, 34, "croatia", "out of contest");
    eurovisionAddState(eurovision, 35, "romania", "out of contest");
    eurovisionAddState(eurovision, 36, "iceland", "out of contest");
    eurovisionAddState(eurovision, 37, "belgium", "out of contest");
    eurovisionAddState(eurovision, 38, "poland", "out of contest");
    eurovisionAddState(eurovision, 39, "armenia", "out of contest");
    eurovisionAddState(eurovision, 40, "greece", "out of contest");
    eurovisionAddState(eurovision, 41, "montenegro", "out of contest");
    eurovisionAddState(eurovision, 42, "russia", "out of contest");
    eurovisionAddState(eurovision, 43, "switzerland", "out of contest");
}

static int *makeJudgeResults(int id0, int id1, int id2, int id3, int id4,
                             int id5, int id6, int id7, int id8, int id9) {
    int *results = malloc(POINTS_OPTIONS_NUMBER * sizeof(*results));
    assert(results);
    results[0] = id0;
    results[1] = id1;
    results[2] = id2;
    results[3] = id3;
    results[4] = id4;
    results[5] = id5;
    results[6] = id6;
    results[7] = id7;
    results[8] = id8;
    results[9] = id9;
    return results;
}

/** total judge result: (stateid: score)
 * 0:  12 + 2  + 0  = 14
 * 1:  10 + 0  + 0  = 10
 * 2:  8  + 1  + 0  = 9
 * 3:  7  + 0  + 7  = 14
 * 4:  6  + 6  + 6  = 18
 * 5:  5  + 5  + 5  = 15
 * 6:  4  + 4  + 4  = 12
 * 7:  3  + 0  + 3  = 6
 * 8:  2  + 0  + 2  = 4
 * 9:  1  + 0  + 1  = 2
 * 10: 0  + 8  + 12 = 20
 * 11: 0  + 0  + 10 = 10
 * 12: 0  + 0  + 8  = 8
 * 13: 0  + 10 + 0  = 12
 * 14: 0  + 12 + 0  = 12
 * 15: 0  + 3  + 0  = 3
 */
static void setupEurovisionJudges(Eurovision eurovision) {
    int *results;
    results = makeJudgeResults(13, 22, 23, 5, 20, 3, 14, 15, 16, 6);
    eurovisionAddJudge(eurovision,1 ,"loooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooong name" , results);
    free(results);
    results = makeJudgeResults(25, 22, 5, 11, 13, 24, 14, 26, 20, 6);
    eurovisionAddJudge(eurovision,2 ," name" , results);
    free(results);
    results = makeJudgeResults(20, 5, 25, 23, 19, 6, 12, 14, 13, 22);
    eurovisionAddJudge(eurovision,3 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 13, 20, 8, 22, 11, 24, 23, 21, 25);
    eurovisionAddJudge(eurovision,4 ,"jury" , results);
    free(results);
    results = makeJudgeResults(22, 11, 20, 12, 18, 3, 24, 14, 6, 23);
    eurovisionAddJudge(eurovision,5 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 4, 25, 18, 11, 20, 7, 8, 17, 23);
    eurovisionAddJudge(eurovision,6 ,"jury" , results);
    free(results);
    results = makeJudgeResults(11, 20, 5, 13, 16, 25, 23, 4, 2, 24);
    eurovisionAddJudge(eurovision,7 ,"jury" , results);
    free(results);
    results = makeJudgeResults(6, 12, 5, 18, 4, 13, 26, 3, 2, 22);
    eurovisionAddJudge(eurovision,8 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 22, 18, 12, 6, 7, 17, 24, 20, 2);
    eurovisionAddJudge(eurovision,9 ,"jury" , results);
    free(results);
    results = makeJudgeResults(20, 11, 26, 23, 7, 19, 5, 6, 22, 12);
    eurovisionAddJudge(eurovision,10 ,"jury" , results);
    free(results);
    results = makeJudgeResults(20, 5, 24, 16, 2, 23, 4, 25, 6, 22);
    eurovisionAddJudge(eurovision,11 ,"jury" , results);
    free(results);
    results = makeJudgeResults(26, 25, 18, 13, 11, 22, 20, 10, 19, 14);
    eurovisionAddJudge(eurovision,12 ,"jury" , results);
    free(results);
    results = makeJudgeResults(22, 16, 11, 5, 23, 20, 14, 9, 3, 24);
    eurovisionAddJudge(eurovision,13 ,"jury" , results);
    free(results);
    results = makeJudgeResults(22, 24, 20, 3, 12, 5, 18, 17, 21, 4);
    eurovisionAddJudge(eurovision,14 ,"jury" , results);
    free(results);
    results = makeJudgeResults(11, 16, 5, 6, 2, 25, 20, 22, 13, 18);
    eurovisionAddJudge(eurovision,15 ,"jury" , results);
    free(results);
    results = makeJudgeResults(20, 11, 6, 2, 22, 5, 24, 4, 19, 14);
    eurovisionAddJudge(eurovision,16 ,"jury" , results);
    free(results);
    results = makeJudgeResults(22, 18, 20, 5, 25, 13, 26, 15, 24, 11);
    eurovisionAddJudge(eurovision,17 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 4, 14, 12, 11, 13, 22, 25, 20, 24);
    eurovisionAddJudge(eurovision,18 ,"jury" , results);
    free(results);
    results = makeJudgeResults(6, 22, 18, 16, 25, 1, 11, 5, 7, 23);
    eurovisionAddJudge(eurovision,19 ,"jury" , results);
    free(results);
    results = makeJudgeResults(25, 5, 16, 22, 18, 13, 24, 17, 7, 11);
    eurovisionAddJudge(eurovision,20 ,"jury" , results);
    free(results);
    results = makeJudgeResults(15, 12, 5, 16, 22, 4, 23, 24, 7, 20);
    eurovisionAddJudge(eurovision,21 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 20, 9, 16, 17, 11, 3, 21, 15, 18);
    eurovisionAddJudge(eurovision,22 ,"jury" , results);
    free(results);
    results = makeJudgeResults(11, 5, 20, 4, 25, 22, 6, 7, 8, 3);
    eurovisionAddJudge(eurovision,23 ,"jury" , results);
    free(results);
    results = makeJudgeResults(25, 18, 11, 22, 8, 5, 13, 6, 12, 2);
    eurovisionAddJudge(eurovision,24 ,"jury" , results);
    free(results);
    results = makeJudgeResults(20, 19, 26, 2, 12, 14, 21, 13, 5, 6);
    eurovisionAddJudge(eurovision,25 ,"jury" , results);
    free(results);
    results = makeJudgeResults(7, 11, 15, 5, 9, 24, 6, 25, 22, 20);
    eurovisionAddJudge(eurovision,26 ,"jury" , results);
    free(results);
    results = makeJudgeResults(12, 10, 21, 19, 1, 18, 25, 6, 11, 22);
    eurovisionAddJudge(eurovision,27 ,"jury" , results);
    free(results);
    results = makeJudgeResults(25, 5, 7, 12, 14, 4, 3, 15, 16, 20);
    eurovisionAddJudge(eurovision,28 ,"jury" , results);
    free(results);
    results = makeJudgeResults(22, 11, 20, 19, 3, 6, 26, 10, 16, 24);
    eurovisionAddJudge(eurovision,29 ,"jury" , results);
    free(results);
    results = makeJudgeResults(6, 25, 10, 20, 12, 17, 14, 11, 3, 22);
    eurovisionAddJudge(eurovision,30 ,"jury" , results);
    free(results);
    results = makeJudgeResults(25, 26, 13, 20, 22, 14, 7, 16, 18, 5);
    eurovisionAddJudge(eurovision,31 ,"jury" , results);
    free(results);
    results = makeJudgeResults(20, 6, 5, 11, 18, 23, 12, 22, 4, 15);
    eurovisionAddJudge(eurovision,32 ,"jury" , results);
    free(results);
    results = makeJudgeResults(20, 13, 6, 5, 16, 23, 4, 11, 9, 3);
    eurovisionAddJudge(eurovision,33 ,"jury" , results);
    free(results);
    results = makeJudgeResults(4, 22, 19, 18, 13, 7, 14, 24, 9, 12);
    eurovisionAddJudge(eurovision,34 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 2, 23, 19, 4, 25, 11, 6, 16, 3);
    eurovisionAddJudge(eurovision,35 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 12, 22, 6, 11, 20, 24, 13, 25, 14);
    eurovisionAddJudge(eurovision,36 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 23, 20, 14, 22, 11, 25, 13, 12, 2);
    eurovisionAddJudge(eurovision,37 ,"jury" , results);
    free(results);
    results = makeJudgeResults(5, 11, 23, 12, 20, 24, 16, 21, 18, 4);
    eurovisionAddJudge(eurovision,38 ,"jury" , results);
    free(results);
    results = makeJudgeResults(20, 19, 22, 25, 14, 11, 13, 23, 16, 5);
    eurovisionAddJudge(eurovision,39 ,"jury" , results);
    free(results);
    results = makeJudgeResults(25, 19, 20, 12, 21, 13, 22, 6, 10, 26);
    eurovisionAddJudge(eurovision,40 ,"jury" , results);
    free(results);
    results = makeJudgeResults(10, 12, 19, 7, 15, 6, 26, 23, 9, 25);
    eurovisionAddJudge(eurovision,41 ,"jury" , results);
    free(results);
    results = makeJudgeResults(19, 20, 22, 25, 6, 16, 11, 4, 13, 26);
    eurovisionAddJudge(eurovision,42 ,"jury" , results);
    free(results);
    results = makeJudgeResults(11, 6, 4, 25, 24, 20, 5, 8, 18, 22);
    eurovisionAddJudge(eurovision,43 ,"jury" , results);
    free(results);


}

static void giveVotes(Eurovision eurovision, int giver, int taker, int votes) {
    for (int i = 0; i < votes; i++) {
        if (eurovisionAddVote(eurovision, giver, taker) != EUROVISION_SUCCESS) {
            printf("error in eurovisionAddVote %d -> %d\n", giver, taker);
        }
    }
}

static void setupEurovisionVotes(Eurovision eurovision) {
    giveVotes(eurovision, 1, 12, 4);
    giveVotes(eurovision, 2, 8, 2);
    giveVotes(eurovision, 3, 4, 1);
}

/** total voters result:
 * 1:  12 + 0  + 0       = 12
 * 2:  12 + 0  + 8       = 20
 * 3:  10 + 6  + 0  + 12 = 28
 * 4:  7  + 7  + 12      = 26
 * 5:  8  + 8  + 4       = 20
 * 6:  6  + 10 + 7       = 23
 * 7:  5  + 5  + 5       = 15
 * 8:  4  + 0  + 6       = 10
 * 9:  3  + 0  + 3       = 6
 * 10: 2  + 0  + 10      = 12
 * 11: 1  + 0  + 0       = 1
 * 13: 0  + 0  + 1       = 1
 * 14: 0  + 0  + 2       = 2
 */
static void setupEurovisionVotes2(Eurovision eurovision) {

    giveVotes(eurovision, 1, 1, 0);
    giveVotes(eurovision, 1, 2, 620);
    giveVotes(eurovision, 1, 3, 720);
    giveVotes(eurovision, 1, 4, 828);
    giveVotes(eurovision, 1, 5, 106);
    giveVotes(eurovision, 1, 6, 832);
    giveVotes(eurovision, 1, 7, 538);
    giveVotes(eurovision, 1, 8, 416);
    giveVotes(eurovision, 1, 9, 79);
    giveVotes(eurovision, 1, 10, 609);
    giveVotes(eurovision, 1, 11, 11);
    giveVotes(eurovision, 1, 12, 406);
    giveVotes(eurovision, 1, 13, 913);
    giveVotes(eurovision, 1, 14, 946);
    giveVotes(eurovision, 1, 15, 941);
    giveVotes(eurovision, 1, 16, 432);
    giveVotes(eurovision, 1, 17, 201);
    giveVotes(eurovision, 1, 18, 147);
    giveVotes(eurovision, 1, 19, 906);
    giveVotes(eurovision, 1, 20, 648);
    giveVotes(eurovision, 1, 21, 756);
    giveVotes(eurovision, 1, 22, 986);
    giveVotes(eurovision, 1, 23, 137);
    giveVotes(eurovision, 1, 24, 40);
    giveVotes(eurovision, 1, 25, 834);
    giveVotes(eurovision, 1, 26, 874);
    giveVotes(eurovision, 1, 27, 35);
    giveVotes(eurovision, 1, 28, 55);
    giveVotes(eurovision, 1, 29, 382);
    giveVotes(eurovision, 1, 30, 456);
    giveVotes(eurovision, 1, 31, 730);
    giveVotes(eurovision, 1, 32, 261);
    giveVotes(eurovision, 1, 33, 685);
    giveVotes(eurovision, 1, 34, 464);
    giveVotes(eurovision, 1, 35, 486);
    giveVotes(eurovision, 1, 36, 439);
    giveVotes(eurovision, 1, 37, 466);
    giveVotes(eurovision, 1, 38, 51);
    giveVotes(eurovision, 1, 39, 493);
    giveVotes(eurovision, 1, 40, 258);
    giveVotes(eurovision, 1, 41, 403);
    giveVotes(eurovision, 1, 42, 506);
    giveVotes(eurovision, 1, 43, 245);

    giveVotes(eurovision, 2, 1, 753);
    giveVotes(eurovision, 2, 2, 0);
    giveVotes(eurovision, 2, 3, 233);
    giveVotes(eurovision, 2, 4, 392);
    giveVotes(eurovision, 2, 5, 44);
    giveVotes(eurovision, 2, 6, 797);
    giveVotes(eurovision, 2, 7, 799);
    giveVotes(eurovision, 2, 8, 726);
    giveVotes(eurovision, 2, 9, 412);
    giveVotes(eurovision, 2, 10, 73);
    giveVotes(eurovision, 2, 11, 864);
    giveVotes(eurovision, 2, 12, 453);
    giveVotes(eurovision, 2, 13, 844);
    giveVotes(eurovision, 2, 14, 972);
    giveVotes(eurovision, 2, 15, 699);
    giveVotes(eurovision, 2, 16, 357);
    giveVotes(eurovision, 2, 17, 760);
    giveVotes(eurovision, 2, 18, 846);
    giveVotes(eurovision, 2, 19, 406);
    giveVotes(eurovision, 2, 20, 91);
    giveVotes(eurovision, 2, 21, 5);
    giveVotes(eurovision, 2, 22, 973);
    giveVotes(eurovision, 2, 23, 418);
    giveVotes(eurovision, 2, 24, 770);
    giveVotes(eurovision, 2, 25, 967);
    giveVotes(eurovision, 2, 26, 944);
    giveVotes(eurovision, 2, 27, 584);
    giveVotes(eurovision, 2, 28, 264);
    giveVotes(eurovision, 2, 29, 115);
    giveVotes(eurovision, 2, 30, 394);
    giveVotes(eurovision, 2, 31, 504);
    giveVotes(eurovision, 2, 32, 625);
    giveVotes(eurovision, 2, 33, 217);
    giveVotes(eurovision, 2, 34, 459);
    giveVotes(eurovision, 2, 35, 425);
    giveVotes(eurovision, 2, 36, 764);
    giveVotes(eurovision, 2, 37, 653);
    giveVotes(eurovision, 2, 38, 492);
    giveVotes(eurovision, 2, 39, 98);
    giveVotes(eurovision, 2, 40, 368);
    giveVotes(eurovision, 2, 41, 640);
    giveVotes(eurovision, 2, 42, 501);
    giveVotes(eurovision, 2, 43, 387);

    giveVotes(eurovision, 3, 1, 192);
    giveVotes(eurovision, 3, 2, 398);
    giveVotes(eurovision, 3, 3, 0);
    giveVotes(eurovision, 3, 4, 347);
    giveVotes(eurovision, 3, 5, 711);
    giveVotes(eurovision, 3, 6, 403);
    giveVotes(eurovision, 3, 7, 825);
    giveVotes(eurovision, 3, 8, 642);
    giveVotes(eurovision, 3, 9, 504);
    giveVotes(eurovision, 3, 10, 961);
    giveVotes(eurovision, 3, 11, 723);
    giveVotes(eurovision, 3, 12, 681);
    giveVotes(eurovision, 3, 13, 553);
    giveVotes(eurovision, 3, 14, 909);
    giveVotes(eurovision, 3, 15, 890);
    giveVotes(eurovision, 3, 16, 222);
    giveVotes(eurovision, 3, 17, 444);
    giveVotes(eurovision, 3, 18, 8);
    giveVotes(eurovision, 3, 19, 447);
    giveVotes(eurovision, 3, 20, 289);
    giveVotes(eurovision, 3, 21, 716);
    giveVotes(eurovision, 3, 22, 335);
    giveVotes(eurovision, 3, 23, 192);
    giveVotes(eurovision, 3, 24, 7);
    giveVotes(eurovision, 3, 25, 882);
    giveVotes(eurovision, 3, 26, 930);
    giveVotes(eurovision, 3, 27, 357);
    giveVotes(eurovision, 3, 28, 355);
    giveVotes(eurovision, 3, 29, 443);
    giveVotes(eurovision, 3, 30, 473);
    giveVotes(eurovision, 3, 31, 446);
    giveVotes(eurovision, 3, 32, 85);
    giveVotes(eurovision, 3, 33, 421);
    giveVotes(eurovision, 3, 34, 168);
    giveVotes(eurovision, 3, 35, 561);
    giveVotes(eurovision, 3, 36, 268);
    giveVotes(eurovision, 3, 37, 236);
    giveVotes(eurovision, 3, 38, 68);
    giveVotes(eurovision, 3, 39, 631);
    giveVotes(eurovision, 3, 40, 641);
    giveVotes(eurovision, 3, 41, 588);
    giveVotes(eurovision, 3, 42, 216);
    giveVotes(eurovision, 3, 43, 554);

    giveVotes(eurovision, 4, 1, 543);
    giveVotes(eurovision, 4, 2, 476);
    giveVotes(eurovision, 4, 3, 355);
    giveVotes(eurovision, 4, 4, 0);
    giveVotes(eurovision, 4, 5, 679);
    giveVotes(eurovision, 4, 6, 944);
    giveVotes(eurovision, 4, 7, 642);
    giveVotes(eurovision, 4, 8, 581);
    giveVotes(eurovision, 4, 9, 637);
    giveVotes(eurovision, 4, 10, 617);
    giveVotes(eurovision, 4, 11, 667);
    giveVotes(eurovision, 4, 12, 136);
    giveVotes(eurovision, 4, 13, 744);
    giveVotes(eurovision, 4, 14, 852);
    giveVotes(eurovision, 4, 15, 916);
    giveVotes(eurovision, 4, 16, 629);
    giveVotes(eurovision, 4, 17, 614);
    giveVotes(eurovision, 4, 18, 290);
    giveVotes(eurovision, 4, 19, 462);
    giveVotes(eurovision, 4, 20, 695);
    giveVotes(eurovision, 4, 21, 286);
    giveVotes(eurovision, 4, 22, 659);
    giveVotes(eurovision, 4, 23, 298);
    giveVotes(eurovision, 4, 24, 447);
    giveVotes(eurovision, 4, 25, 767);
    giveVotes(eurovision, 4, 26, 807);
    giveVotes(eurovision, 4, 27, 619);
    giveVotes(eurovision, 4, 28, 19);
    giveVotes(eurovision, 4, 29, 151);
    giveVotes(eurovision, 4, 30, 57);
    giveVotes(eurovision, 4, 31, 508);
    giveVotes(eurovision, 4, 32, 3);
    giveVotes(eurovision, 4, 33, 214);
    giveVotes(eurovision, 4, 34, 50);
    giveVotes(eurovision, 4, 35, 601);
    giveVotes(eurovision, 4, 36, 234);
    giveVotes(eurovision, 4, 37, 481);
    giveVotes(eurovision, 4, 38, 569);
    giveVotes(eurovision, 4, 39, 594);
    giveVotes(eurovision, 4, 40, 180);
    giveVotes(eurovision, 4, 41, 479);
    giveVotes(eurovision, 4, 42, 656);
    giveVotes(eurovision, 4, 43, 137);

    giveVotes(eurovision, 5, 1, 266);
    giveVotes(eurovision, 5, 2, 61);
    giveVotes(eurovision, 5, 3, 472);
    giveVotes(eurovision, 5, 4, 698);
    giveVotes(eurovision, 5, 5, 0);
    giveVotes(eurovision, 5, 6, 474);
    giveVotes(eurovision, 5, 7, 235);
    giveVotes(eurovision, 5, 8, 124);
    giveVotes(eurovision, 5, 9, 592);
    giveVotes(eurovision, 5, 10, 963);
    giveVotes(eurovision, 5, 11, 941);
    giveVotes(eurovision, 5, 12, 804);
    giveVotes(eurovision, 5, 13, 292);
    giveVotes(eurovision, 5, 14, 981);
    giveVotes(eurovision, 5, 15, 940);
    giveVotes(eurovision, 5, 16, 720);
    giveVotes(eurovision, 5, 17, 470);
    giveVotes(eurovision, 5, 18, 514);
    giveVotes(eurovision, 5, 19, 620);
    giveVotes(eurovision, 5, 20, 304);
    giveVotes(eurovision, 5, 21, 811);
    giveVotes(eurovision, 5, 22, 948);
    giveVotes(eurovision, 5, 23, 309);
    giveVotes(eurovision, 5, 24, 884);
    giveVotes(eurovision, 5, 25, 774);
    giveVotes(eurovision, 5, 26, 969);
    giveVotes(eurovision, 5, 27, 13);
    giveVotes(eurovision, 5, 28, 74);
    giveVotes(eurovision, 5, 29, 4);
    giveVotes(eurovision, 5, 30, 290);
    giveVotes(eurovision, 5, 31, 462);
    giveVotes(eurovision, 5, 32, 357);
    giveVotes(eurovision, 5, 33, 479);
    giveVotes(eurovision, 5, 34, 51);
    giveVotes(eurovision, 5, 35, 686);
    giveVotes(eurovision, 5, 36, 247);
    giveVotes(eurovision, 5, 37, 0);
    giveVotes(eurovision, 5, 38, 666);
    giveVotes(eurovision, 5, 39, 80);
    giveVotes(eurovision, 5, 40, 57);
    giveVotes(eurovision, 5, 41, 188);
    giveVotes(eurovision, 5, 42, 337);
    giveVotes(eurovision, 5, 43, 310);

    giveVotes(eurovision, 6, 1, 194);
    giveVotes(eurovision, 6, 2, 167);
    giveVotes(eurovision, 6, 3, 746);
    giveVotes(eurovision, 6, 4, 934);
    giveVotes(eurovision, 6, 5, 899);
    giveVotes(eurovision, 6, 6, 0);
    giveVotes(eurovision, 6, 7, 803);
    giveVotes(eurovision, 6, 8, 754);
    giveVotes(eurovision, 6, 9, 125);
    giveVotes(eurovision, 6, 10, 799);
    giveVotes(eurovision, 6, 11, 634);
    giveVotes(eurovision, 6, 12, 41);
    giveVotes(eurovision, 6, 13, 59);
    giveVotes(eurovision, 6, 14, 892);
    giveVotes(eurovision, 6, 15, 914);
    giveVotes(eurovision, 6, 16, 660);
    giveVotes(eurovision, 6, 17, 930);
    giveVotes(eurovision, 6, 18, 350);
    giveVotes(eurovision, 6, 19, 801);
    giveVotes(eurovision, 6, 20, 174);
    giveVotes(eurovision, 6, 21, 836);
    giveVotes(eurovision, 6, 22, 76);
    giveVotes(eurovision, 6, 23, 334);
    giveVotes(eurovision, 6, 24, 547);
    giveVotes(eurovision, 6, 25, 885);
    giveVotes(eurovision, 6, 26, 911);
    giveVotes(eurovision, 6, 27, 558);
    giveVotes(eurovision, 6, 28, 134);
    giveVotes(eurovision, 6, 29, 602);
    giveVotes(eurovision, 6, 30, 224);
    giveVotes(eurovision, 6, 31, 565);
    giveVotes(eurovision, 6, 32, 663);
    giveVotes(eurovision, 6, 33, 484);
    giveVotes(eurovision, 6, 34, 542);
    giveVotes(eurovision, 6, 35, 492);
    giveVotes(eurovision, 6, 36, 248);
    giveVotes(eurovision, 6, 37, 654);
    giveVotes(eurovision, 6, 38, 583);
    giveVotes(eurovision, 6, 39, 442);
    giveVotes(eurovision, 6, 40, 236);
    giveVotes(eurovision, 6, 41, 358);
    giveVotes(eurovision, 6, 42, 614);
    giveVotes(eurovision, 6, 43, 363);

    giveVotes(eurovision, 7, 1, 752);
    giveVotes(eurovision, 7, 2, 516);
    giveVotes(eurovision, 7, 3, 203);
    giveVotes(eurovision, 7, 4, 996);
    giveVotes(eurovision, 7, 5, 973);
    giveVotes(eurovision, 7, 6, 390);
    giveVotes(eurovision, 7, 7, 0);
    giveVotes(eurovision, 7, 8, 479);
    giveVotes(eurovision, 7, 9, 430);
    giveVotes(eurovision, 7, 10, 736);
    giveVotes(eurovision, 7, 11, 959);
    giveVotes(eurovision, 7, 12, 446);
    giveVotes(eurovision, 7, 13, 815);
    giveVotes(eurovision, 7, 14, 924);
    giveVotes(eurovision, 7, 15, 977);
    giveVotes(eurovision, 7, 16, 354);
    giveVotes(eurovision, 7, 17, 487);
    giveVotes(eurovision, 7, 18, 274);
    giveVotes(eurovision, 7, 19, 136);
    giveVotes(eurovision, 7, 20, 906);
    giveVotes(eurovision, 7, 21, 654);
    giveVotes(eurovision, 7, 22, 962);
    giveVotes(eurovision, 7, 23, 932);
    giveVotes(eurovision, 7, 24, 12);
    giveVotes(eurovision, 7, 25, 871);
    giveVotes(eurovision, 7, 26, 265);
    giveVotes(eurovision, 7, 27, 648);
    giveVotes(eurovision, 7, 28, 22);
    giveVotes(eurovision, 7, 29, 633);
    giveVotes(eurovision, 7, 30, 3);
    giveVotes(eurovision, 7, 31, 459);
    giveVotes(eurovision, 7, 32, 813);
    giveVotes(eurovision, 7, 33, 537);
    giveVotes(eurovision, 7, 34, 765);
    giveVotes(eurovision, 7, 35, 110);
    giveVotes(eurovision, 7, 36, 164);
    giveVotes(eurovision, 7, 37, 799);
    giveVotes(eurovision, 7, 38, 762);
    giveVotes(eurovision, 7, 39, 100);
    giveVotes(eurovision, 7, 40, 237);
    giveVotes(eurovision, 7, 41, 216);
    giveVotes(eurovision, 7, 42, 29);
    giveVotes(eurovision, 7, 43, 734);

    giveVotes(eurovision, 8, 1, 829);
    giveVotes(eurovision, 8, 2, 968);
    giveVotes(eurovision, 8, 3, 61);
    giveVotes(eurovision, 8, 4, 63);
    giveVotes(eurovision, 8, 5, 405);
    giveVotes(eurovision, 8, 6, 870);
    giveVotes(eurovision, 8, 7, 20);
    giveVotes(eurovision, 8, 8, 0);
    giveVotes(eurovision, 8, 9, 584);
    giveVotes(eurovision, 8, 10, 59);
    giveVotes(eurovision, 8, 11, 888);
    giveVotes(eurovision, 8, 12, 94);
    giveVotes(eurovision, 8, 13, 664);
    giveVotes(eurovision, 8, 14, 178);
    giveVotes(eurovision, 8, 15, 720);
    giveVotes(eurovision, 8, 16, 139);
    giveVotes(eurovision, 8, 17, 559);
    giveVotes(eurovision, 8, 18, 272);
    giveVotes(eurovision, 8, 19, 863);
    giveVotes(eurovision, 8, 20, 553);
    giveVotes(eurovision, 8, 21, 331);
    giveVotes(eurovision, 8, 22, 703);
    giveVotes(eurovision, 8, 23, 121);
    giveVotes(eurovision, 8, 24, 743);
    giveVotes(eurovision, 8, 25, 851);
    giveVotes(eurovision, 8, 26, 899);
    giveVotes(eurovision, 8, 27, 445);
    giveVotes(eurovision, 8, 28, 22);
    giveVotes(eurovision, 8, 29, 208);
    giveVotes(eurovision, 8, 30, 278);
    giveVotes(eurovision, 8, 31, 201);
    giveVotes(eurovision, 8, 32, 55);
    giveVotes(eurovision, 8, 33, 651);
    giveVotes(eurovision, 8, 34, 19);
    giveVotes(eurovision, 8, 35, 578);
    giveVotes(eurovision, 8, 36, 389);
    giveVotes(eurovision, 8, 37, 528);
    giveVotes(eurovision, 8, 38, 131);
    giveVotes(eurovision, 8, 39, 460);
    giveVotes(eurovision, 8, 40, 675);
    giveVotes(eurovision, 8, 41, 188);
    giveVotes(eurovision, 8, 42, 38);
    giveVotes(eurovision, 8, 43, 57);

    giveVotes(eurovision, 9, 1, 34);
    giveVotes(eurovision, 9, 2, 406);
    giveVotes(eurovision, 9, 3, 240);
    giveVotes(eurovision, 9, 4, 981);
    giveVotes(eurovision, 9, 5, 472);
    giveVotes(eurovision, 9, 6, 128);
    giveVotes(eurovision, 9, 7, 404);
    giveVotes(eurovision, 9, 8, 519);
    giveVotes(eurovision, 9, 9, 0);
    giveVotes(eurovision, 9, 10, 357);
    giveVotes(eurovision, 9, 11, 809);
    giveVotes(eurovision, 9, 12, 0);
    giveVotes(eurovision, 9, 13, 365);
    giveVotes(eurovision, 9, 14, 881);
    giveVotes(eurovision, 9, 15, 790);
    giveVotes(eurovision, 9, 16, 738);
    giveVotes(eurovision, 9, 17, 366);
    giveVotes(eurovision, 9, 18, 886);
    giveVotes(eurovision, 9, 19, 818);
    giveVotes(eurovision, 9, 20, 25);
    giveVotes(eurovision, 9, 21, 574);
    giveVotes(eurovision, 9, 22, 923);
    giveVotes(eurovision, 9, 23, 109);
    giveVotes(eurovision, 9, 24, 980);
    giveVotes(eurovision, 9, 25, 956);
    giveVotes(eurovision, 9, 26, 360);
    giveVotes(eurovision, 9, 27, 20);
    giveVotes(eurovision, 9, 28, 397);
    giveVotes(eurovision, 9, 29, 577);
    giveVotes(eurovision, 9, 30, 495);
    giveVotes(eurovision, 9, 31, 251);
    giveVotes(eurovision, 9, 32, 320);
    giveVotes(eurovision, 9, 33, 352);
    giveVotes(eurovision, 9, 34, 665);
    giveVotes(eurovision, 9, 35, 640);
    giveVotes(eurovision, 9, 36, 315);
    giveVotes(eurovision, 9, 37, 44);
    giveVotes(eurovision, 9, 38, 223);
    giveVotes(eurovision, 9, 39, 396);
    giveVotes(eurovision, 9, 40, 517);
    giveVotes(eurovision, 9, 41, 6);
    giveVotes(eurovision, 9, 42, 510);
    giveVotes(eurovision, 9, 43, 585);

    giveVotes(eurovision, 10, 1, 521);
    giveVotes(eurovision, 10, 2, 748);
    giveVotes(eurovision, 10, 3, 927);
    giveVotes(eurovision, 10, 4, 341);
    giveVotes(eurovision, 10, 5, 824);
    giveVotes(eurovision, 10, 6, 254);
    giveVotes(eurovision, 10, 7, 872);
    giveVotes(eurovision, 10, 8, 783);
    giveVotes(eurovision, 10, 9, 775);
    giveVotes(eurovision, 10, 10, 0);
    giveVotes(eurovision, 10, 11, 696);
    giveVotes(eurovision, 10, 12, 405);
    giveVotes(eurovision, 10, 13, 319);
    giveVotes(eurovision, 10, 14, 893);
    giveVotes(eurovision, 10, 15, 877);
    giveVotes(eurovision, 10, 16, 631);
    giveVotes(eurovision, 10, 17, 297);
    giveVotes(eurovision, 10, 18, 862);
    giveVotes(eurovision, 10, 19, 849);
    giveVotes(eurovision, 10, 20, 256);
    giveVotes(eurovision, 10, 21, 946);
    giveVotes(eurovision, 10, 22, 904);
    giveVotes(eurovision, 10, 23, 5);
    giveVotes(eurovision, 10, 24, 155);
    giveVotes(eurovision, 10, 25, 943);
    giveVotes(eurovision, 10, 26, 902);
    giveVotes(eurovision, 10, 27, 550);
    giveVotes(eurovision, 10, 28, 177);
    giveVotes(eurovision, 10, 29, 366);
    giveVotes(eurovision, 10, 30, 102);
    giveVotes(eurovision, 10, 31, 712);
    giveVotes(eurovision, 10, 32, 465);
    giveVotes(eurovision, 10, 33, 653);
    giveVotes(eurovision, 10, 34, 0);
    giveVotes(eurovision, 10, 35, 537);
    giveVotes(eurovision, 10, 36, 8);
    giveVotes(eurovision, 10, 37, 501);
    giveVotes(eurovision, 10, 38, 207);
    giveVotes(eurovision, 10, 39, 140);
    giveVotes(eurovision, 10, 40, 818);
    giveVotes(eurovision, 10, 41, 525);
    giveVotes(eurovision, 10, 42, 422);
    giveVotes(eurovision, 10, 43, 849);

    giveVotes(eurovision, 11, 1, 639);
    giveVotes(eurovision, 11, 2, 176);
    giveVotes(eurovision, 11, 3, 223);
    giveVotes(eurovision, 11, 4, 855);
    giveVotes(eurovision, 11, 5, 867);
    giveVotes(eurovision, 11, 6, 9);
    giveVotes(eurovision, 11, 7, 246);
    giveVotes(eurovision, 11, 8, 36);
    giveVotes(eurovision, 11, 9, 810);
    giveVotes(eurovision, 11, 10, 383);
    giveVotes(eurovision, 11, 11, 0);
    giveVotes(eurovision, 11, 12, 232);
    giveVotes(eurovision, 11, 13, 103);
    giveVotes(eurovision, 11, 14, 891);
    giveVotes(eurovision, 11, 15, 840);
    giveVotes(eurovision, 11, 16, 609);
    giveVotes(eurovision, 11, 17, 773);
    giveVotes(eurovision, 11, 18, 234);
    giveVotes(eurovision, 11, 19, 285);
    giveVotes(eurovision, 11, 20, 94);
    giveVotes(eurovision, 11, 21, 821);
    giveVotes(eurovision, 11, 22, 906);
    giveVotes(eurovision, 11, 23, 161);
    giveVotes(eurovision, 11, 24, 890);
    giveVotes(eurovision, 11, 25, 871);
    giveVotes(eurovision, 11, 26, 985);
    giveVotes(eurovision, 11, 27, 138);
    giveVotes(eurovision, 11, 28, 87);
    giveVotes(eurovision, 11, 29, 38);
    giveVotes(eurovision, 11, 30, 624);
    giveVotes(eurovision, 11, 31, 132);
    giveVotes(eurovision, 11, 32, 3);
    giveVotes(eurovision, 11, 33, 613);
    giveVotes(eurovision, 11, 34, 182);
    giveVotes(eurovision, 11, 35, 118);
    giveVotes(eurovision, 11, 36, 417);
    giveVotes(eurovision, 11, 37, 429);
    giveVotes(eurovision, 11, 38, 55);
    giveVotes(eurovision, 11, 39, 644);
    giveVotes(eurovision, 11, 40, 210);
    giveVotes(eurovision, 11, 41, 37);
    giveVotes(eurovision, 11, 42, 160);
    giveVotes(eurovision, 11, 43, 548);

    giveVotes(eurovision, 12, 1, 157);
    giveVotes(eurovision, 12, 2, 692);
    giveVotes(eurovision, 12, 3, 170);
    giveVotes(eurovision, 12, 4, 335);
    giveVotes(eurovision, 12, 5, 874);
    giveVotes(eurovision, 12, 6, 920);
    giveVotes(eurovision, 12, 7, 9);
    giveVotes(eurovision, 12, 8, 357);
    giveVotes(eurovision, 12, 9, 917);
    giveVotes(eurovision, 12, 10, 360);
    giveVotes(eurovision, 12, 11, 949);
    giveVotes(eurovision, 12, 12, 0);
    giveVotes(eurovision, 12, 13, 924);
    giveVotes(eurovision, 12, 14, 10);
    giveVotes(eurovision, 12, 15, 740);
    giveVotes(eurovision, 12, 16, 715);
    giveVotes(eurovision, 12, 17, 389);
    giveVotes(eurovision, 12, 18, 927);
    giveVotes(eurovision, 12, 19, 225);
    giveVotes(eurovision, 12, 20, 185);
    giveVotes(eurovision, 12, 21, 783);
    giveVotes(eurovision, 12, 22, 863);
    giveVotes(eurovision, 12, 23, 510);
    giveVotes(eurovision, 12, 24, 944);
    giveVotes(eurovision, 12, 25, 950);
    giveVotes(eurovision, 12, 26, 953);
    giveVotes(eurovision, 12, 27, 541);
    giveVotes(eurovision, 12, 28, 762);
    giveVotes(eurovision, 12, 29, 597);
    giveVotes(eurovision, 12, 30, 58);
    giveVotes(eurovision, 12, 31, 277);
    giveVotes(eurovision, 12, 32, 147);
    giveVotes(eurovision, 12, 33, 702);
    giveVotes(eurovision, 12, 34, 855);
    giveVotes(eurovision, 12, 35, 161);
    giveVotes(eurovision, 12, 36, 433);
    giveVotes(eurovision, 12, 37, 575);
    giveVotes(eurovision, 12, 38, 252);
    giveVotes(eurovision, 12, 39, 791);
    giveVotes(eurovision, 12, 40, 547);
    giveVotes(eurovision, 12, 41, 373);
    giveVotes(eurovision, 12, 42, 530);
    giveVotes(eurovision, 12, 43, 123);

    giveVotes(eurovision, 12, 1, 762);
    giveVotes(eurovision, 12, 2, 773);
    giveVotes(eurovision, 12, 3, 626);
    giveVotes(eurovision, 12, 4, 641);
    giveVotes(eurovision, 12, 5, 581);
    giveVotes(eurovision, 12, 6, 871);
    giveVotes(eurovision, 12, 7, 675);
    giveVotes(eurovision, 12, 8, 947);
    giveVotes(eurovision, 12, 9, 603);
    giveVotes(eurovision, 12, 10, 719);
    giveVotes(eurovision, 12, 11, 311);
    giveVotes(eurovision, 12, 12, 0);
    giveVotes(eurovision, 12, 13, 487);
    giveVotes(eurovision, 12, 14, 222);
    giveVotes(eurovision, 12, 15, 728);
    giveVotes(eurovision, 12, 16, 158);
    giveVotes(eurovision, 12, 17, 281);
    giveVotes(eurovision, 12, 18, 144);
    giveVotes(eurovision, 12, 19, 857);
    giveVotes(eurovision, 12, 20, 83);
    giveVotes(eurovision, 12, 21, 567);
    giveVotes(eurovision, 12, 22, 978);
    giveVotes(eurovision, 12, 23, 714);
    giveVotes(eurovision, 12, 24, 142);
    giveVotes(eurovision, 12, 25, 756);
    giveVotes(eurovision, 12, 26, 948);
    giveVotes(eurovision, 12, 27, 31);
    giveVotes(eurovision, 12, 28, 330);
    giveVotes(eurovision, 12, 29, 382);
    giveVotes(eurovision, 12, 30, 452);
    giveVotes(eurovision, 12, 31, 98);
    giveVotes(eurovision, 12, 32, 585);
    giveVotes(eurovision, 12, 33, 685);
    giveVotes(eurovision, 12, 34, 481);
    giveVotes(eurovision, 12, 35, 163);
    giveVotes(eurovision, 12, 36, 513);
    giveVotes(eurovision, 12, 37, 284);
    giveVotes(eurovision, 12, 38, 90);
    giveVotes(eurovision, 12, 39, 37);
    giveVotes(eurovision, 12, 40, 598);
    giveVotes(eurovision, 12, 41, 677);
    giveVotes(eurovision, 12, 42, 26);
    giveVotes(eurovision, 12, 43, 86);

    giveVotes(eurovision, 14, 1, 969);
    giveVotes(eurovision, 14, 2, 621);
    giveVotes(eurovision, 14, 3, 6);
    giveVotes(eurovision, 14, 4, 653);
    giveVotes(eurovision, 14, 5, 774);
    giveVotes(eurovision, 14, 6, 918);
    giveVotes(eurovision, 14, 7, 72);
    giveVotes(eurovision, 14, 8, 122);
    giveVotes(eurovision, 14, 9, 506);
    giveVotes(eurovision, 14, 10, 349);
    giveVotes(eurovision, 14, 11, 879);
    giveVotes(eurovision, 14, 12, 171);
    giveVotes(eurovision, 14, 13, 583);
    giveVotes(eurovision, 14, 14, 0);
    giveVotes(eurovision, 14, 15, 944);
    giveVotes(eurovision, 14, 16, 772);
    giveVotes(eurovision, 14, 17, 129);
    giveVotes(eurovision, 14, 18, 813);
    giveVotes(eurovision, 14, 19, 931);
    giveVotes(eurovision, 14, 20, 199);
    giveVotes(eurovision, 14, 21, 609);
    giveVotes(eurovision, 14, 22, 962);
    giveVotes(eurovision, 14, 23, 508);
    giveVotes(eurovision, 14, 24, 891);
    giveVotes(eurovision, 14, 25, 961);
    giveVotes(eurovision, 14, 26, 846);
    giveVotes(eurovision, 14, 27, 677);
    giveVotes(eurovision, 14, 28, 483);
    giveVotes(eurovision, 14, 29, 431);
    giveVotes(eurovision, 14, 30, 103);
    giveVotes(eurovision, 14, 31, 774);
    giveVotes(eurovision, 14, 32, 474);
    giveVotes(eurovision, 14, 33, 555);
    giveVotes(eurovision, 14, 34, 659);
    giveVotes(eurovision, 14, 35, 473);
    giveVotes(eurovision, 14, 36, 228);
    giveVotes(eurovision, 14, 37, 808);
    giveVotes(eurovision, 14, 38, 710);
    giveVotes(eurovision, 14, 39, 181);
    giveVotes(eurovision, 14, 40, 612);
    giveVotes(eurovision, 14, 41, 637);
    giveVotes(eurovision, 14, 42, 376);
    giveVotes(eurovision, 14, 43, 761);

    giveVotes(eurovision, 15, 1, 174);
    giveVotes(eurovision, 15, 2, 657);
    giveVotes(eurovision, 15, 3, 34);
    giveVotes(eurovision, 15, 4, 696);
    giveVotes(eurovision, 15, 5, 969);
    giveVotes(eurovision, 15, 6, 126);
    giveVotes(eurovision, 15, 7, 911);
    giveVotes(eurovision, 15, 8, 624);
    giveVotes(eurovision, 15, 9, 815);
    giveVotes(eurovision, 15, 10, 649);
    giveVotes(eurovision, 15, 11, 981);
    giveVotes(eurovision, 15, 12, 300);
    giveVotes(eurovision, 15, 13, 574);
    giveVotes(eurovision, 15, 14, 871);
    giveVotes(eurovision, 15, 15, 0);
    giveVotes(eurovision, 15, 16, 781);
    giveVotes(eurovision, 15, 17, 233);
    giveVotes(eurovision, 15, 18, 728);
    giveVotes(eurovision, 15, 19, 352);
    giveVotes(eurovision, 15, 20, 886);
    giveVotes(eurovision, 15, 21, 317);
    giveVotes(eurovision, 15, 22, 493);
    giveVotes(eurovision, 15, 23, 826);
    giveVotes(eurovision, 15, 24, 817);
    giveVotes(eurovision, 15, 25, 774);
    giveVotes(eurovision, 15, 26, 260);
    giveVotes(eurovision, 15, 27, 502);
    giveVotes(eurovision, 15, 28, 472);
    giveVotes(eurovision, 15, 29, 555);
    giveVotes(eurovision, 15, 30, 44);
    giveVotes(eurovision, 15, 31, 52);
    giveVotes(eurovision, 15, 32, 522);
    giveVotes(eurovision, 15, 33, 582);
    giveVotes(eurovision, 15, 34, 32);
    giveVotes(eurovision, 15, 35, 253);
    giveVotes(eurovision, 15, 36, 483);
    giveVotes(eurovision, 15, 37, 364);
    giveVotes(eurovision, 15, 38, 217);
    giveVotes(eurovision, 15, 39, 448);
    giveVotes(eurovision, 15, 40, 408);
    giveVotes(eurovision, 15, 41, 209);
    giveVotes(eurovision, 15, 42, 683);
    giveVotes(eurovision, 15, 43, 185);

    giveVotes(eurovision, 16, 1, 35);
    giveVotes(eurovision, 16, 2, 786);
    giveVotes(eurovision, 16, 3, 498);
    giveVotes(eurovision, 16, 4, 502);
    giveVotes(eurovision, 16, 5, 511);
    giveVotes(eurovision, 16, 6, 729);
    giveVotes(eurovision, 16, 7, 830);
    giveVotes(eurovision, 16, 8, 211);
    giveVotes(eurovision, 16, 9, 919);
    giveVotes(eurovision, 16, 10, 424);
    giveVotes(eurovision, 16, 11, 838);
    giveVotes(eurovision, 16, 12, 603);
    giveVotes(eurovision, 16, 13, 432);
    giveVotes(eurovision, 16, 14, 858);
    giveVotes(eurovision, 16, 15, 947);
    giveVotes(eurovision, 16, 16, 0);
    giveVotes(eurovision, 16, 17, 872);
    giveVotes(eurovision, 16, 18, 725);
    giveVotes(eurovision, 16, 19, 910);
    giveVotes(eurovision, 16, 20, 792);
    giveVotes(eurovision, 16, 21, 577);
    giveVotes(eurovision, 16, 22, 968);
    giveVotes(eurovision, 16, 23, 307);
    giveVotes(eurovision, 16, 24, 947);
    giveVotes(eurovision, 16, 25, 941);
    giveVotes(eurovision, 16, 26, 214);
    giveVotes(eurovision, 16, 27, 315);
    giveVotes(eurovision, 16, 28, 729);
    giveVotes(eurovision, 16, 29, 458);
    giveVotes(eurovision, 16, 30, 435);
    giveVotes(eurovision, 16, 31, 127);
    giveVotes(eurovision, 16, 32, 544);
    giveVotes(eurovision, 16, 33, 324);
    giveVotes(eurovision, 16, 34, 260);
    giveVotes(eurovision, 16, 35, 745);
    giveVotes(eurovision, 16, 36, 614);
    giveVotes(eurovision, 16, 37, 350);
    giveVotes(eurovision, 16, 38, 0);
    giveVotes(eurovision, 16, 39, 268);
    giveVotes(eurovision, 16, 40, 210);
    giveVotes(eurovision, 16, 41, 259);
    giveVotes(eurovision, 16, 42, 551);
    giveVotes(eurovision, 16, 43, 599);

    giveVotes(eurovision, 17, 1, 832);
    giveVotes(eurovision, 17, 2, 280);
    giveVotes(eurovision, 17, 3, 134);
    giveVotes(eurovision, 17, 4, 302);
    giveVotes(eurovision, 17, 5, 887);
    giveVotes(eurovision, 17, 6, 986);
    giveVotes(eurovision, 17, 7, 651);
    giveVotes(eurovision, 17, 8, 340);
    giveVotes(eurovision, 17, 9, 84);
    giveVotes(eurovision, 17, 10, 112);
    giveVotes(eurovision, 17, 11, 601);
    giveVotes(eurovision, 17, 12, 526);
    giveVotes(eurovision, 17, 13, 894);
    giveVotes(eurovision, 17, 14, 894);
    giveVotes(eurovision, 17, 15, 978);
    giveVotes(eurovision, 17, 16, 530);
    giveVotes(eurovision, 17, 17, 0);
    giveVotes(eurovision, 17, 18, 492);
    giveVotes(eurovision, 17, 19, 161);
    giveVotes(eurovision, 17, 20, 38);
    giveVotes(eurovision, 17, 21, 972);
    giveVotes(eurovision, 17, 22, 942);
    giveVotes(eurovision, 17, 23, 884);
    giveVotes(eurovision, 17, 24, 118);
    giveVotes(eurovision, 17, 25, 870);
    giveVotes(eurovision, 17, 26, 927);
    giveVotes(eurovision, 17, 27, 81);
    giveVotes(eurovision, 17, 28, 159);
    giveVotes(eurovision, 17, 29, 495);
    giveVotes(eurovision, 17, 30, 713);
    giveVotes(eurovision, 17, 31, 724);
    giveVotes(eurovision, 17, 32, 867);
    giveVotes(eurovision, 17, 33, 179);
    giveVotes(eurovision, 17, 34, 53);
    giveVotes(eurovision, 17, 35, 204);
    giveVotes(eurovision, 17, 36, 51);
    giveVotes(eurovision, 17, 37, 799);
    giveVotes(eurovision, 17, 38, 441);
    giveVotes(eurovision, 17, 39, 0);
    giveVotes(eurovision, 17, 40, 821);
    giveVotes(eurovision, 17, 41, 701);
    giveVotes(eurovision, 17, 42, 226);
    giveVotes(eurovision, 17, 43, 599);

    giveVotes(eurovision, 18, 1, 793);
    giveVotes(eurovision, 18, 2, 517);
    giveVotes(eurovision, 18, 3, 120);
    giveVotes(eurovision, 18, 4, 247);
    giveVotes(eurovision, 18, 5, 811);
    giveVotes(eurovision, 18, 6, 802);
    giveVotes(eurovision, 18, 7, 603);
    giveVotes(eurovision, 18, 8, 692);
    giveVotes(eurovision, 18, 9, 549);
    giveVotes(eurovision, 18, 10, 891);
    giveVotes(eurovision, 18, 11, 448);
    giveVotes(eurovision, 18, 12, 13);
    giveVotes(eurovision, 18, 13, 279);
    giveVotes(eurovision, 18, 14, 803);
    giveVotes(eurovision, 18, 15, 212);
    giveVotes(eurovision, 18, 16, 769);
    giveVotes(eurovision, 18, 17, 743);
    giveVotes(eurovision, 18, 18, 0);
    giveVotes(eurovision, 18, 19, 907);
    giveVotes(eurovision, 18, 20, 439);
    giveVotes(eurovision, 18, 21, 812);
    giveVotes(eurovision, 18, 22, 952);
    giveVotes(eurovision, 18, 23, 621);
    giveVotes(eurovision, 18, 24, 372);
    giveVotes(eurovision, 18, 25, 990);
    giveVotes(eurovision, 18, 26, 849);
    giveVotes(eurovision, 18, 27, 227);
    giveVotes(eurovision, 18, 28, 260);
    giveVotes(eurovision, 18, 29, 514);
    giveVotes(eurovision, 18, 30, 627);
    giveVotes(eurovision, 18, 31, 424);
    giveVotes(eurovision, 18, 32, 321);
    giveVotes(eurovision, 18, 33, 22);
    giveVotes(eurovision, 18, 34, 675);
    giveVotes(eurovision, 18, 35, 375);
    giveVotes(eurovision, 18, 36, 420);
    giveVotes(eurovision, 18, 37, 209);
    giveVotes(eurovision, 18, 38, 248);
    giveVotes(eurovision, 18, 39, 209);
    giveVotes(eurovision, 18, 40, 117);
    giveVotes(eurovision, 18, 41, 330);
    giveVotes(eurovision, 18, 42, 368);
    giveVotes(eurovision, 18, 43, 683);

    giveVotes(eurovision, 19, 1, 927);
    giveVotes(eurovision, 19, 2, 43);
    giveVotes(eurovision, 19, 3, 98);
    giveVotes(eurovision, 19, 4, 17);
    giveVotes(eurovision, 19, 5, 177);
    giveVotes(eurovision, 19, 6, 719);
    giveVotes(eurovision, 19, 7, 836);
    giveVotes(eurovision, 19, 8, 305);
    giveVotes(eurovision, 19, 9, 418);
    giveVotes(eurovision, 19, 10, 24);
    giveVotes(eurovision, 19, 11, 797);
    giveVotes(eurovision, 19, 12, 112);
    giveVotes(eurovision, 19, 13, 113);
    giveVotes(eurovision, 19, 14, 886);
    giveVotes(eurovision, 19, 15, 630);
    giveVotes(eurovision, 19, 16, 373);
    giveVotes(eurovision, 19, 17, 478);
    giveVotes(eurovision, 19, 18, 735);
    giveVotes(eurovision, 19, 19, 0);
    giveVotes(eurovision, 19, 20, 469);
    giveVotes(eurovision, 19, 21, 734);
    giveVotes(eurovision, 19, 22, 944);
    giveVotes(eurovision, 19, 23, 75);
    giveVotes(eurovision, 19, 24, 597);
    giveVotes(eurovision, 19, 25, 903);
    giveVotes(eurovision, 19, 26, 919);
    giveVotes(eurovision, 19, 27, 505);
    giveVotes(eurovision, 19, 28, 112);
    giveVotes(eurovision, 19, 29, 496);
    giveVotes(eurovision, 19, 30, 494);
    giveVotes(eurovision, 19, 31, 47);
    giveVotes(eurovision, 19, 32, 332);
    giveVotes(eurovision, 19, 33, 288);
    giveVotes(eurovision, 19, 34, 261);
    giveVotes(eurovision, 19, 35, 533);
    giveVotes(eurovision, 19, 36, 363);
    giveVotes(eurovision, 19, 37, 379);
    giveVotes(eurovision, 19, 38, 140);
    giveVotes(eurovision, 19, 39, 19);
    giveVotes(eurovision, 19, 40, 406);
    giveVotes(eurovision, 19, 41, 93);
    giveVotes(eurovision, 19, 42, 581);
    giveVotes(eurovision, 19, 43, 449);

    giveVotes(eurovision, 20, 1, 723);
    giveVotes(eurovision, 20, 2, 76);
    giveVotes(eurovision, 20, 3, 660);
    giveVotes(eurovision, 20, 4, 968);
    giveVotes(eurovision, 20, 5, 860);
    giveVotes(eurovision, 20, 6, 211);
    giveVotes(eurovision, 20, 7, 972);
    giveVotes(eurovision, 20, 8, 676);
    giveVotes(eurovision, 20, 9, 292);
    giveVotes(eurovision, 20, 10, 669);
    giveVotes(eurovision, 20, 11, 885);
    giveVotes(eurovision, 20, 12, 493);
    giveVotes(eurovision, 20, 13, 624);
    giveVotes(eurovision, 20, 14, 863);
    giveVotes(eurovision, 20, 15, 997);
    giveVotes(eurovision, 20, 16, 811);
    giveVotes(eurovision, 20, 17, 931);
    giveVotes(eurovision, 20, 18, 401);
    giveVotes(eurovision, 20, 19, 283);
    giveVotes(eurovision, 20, 20, 0);
    giveVotes(eurovision, 20, 21, 415);
    giveVotes(eurovision, 20, 22, 983);
    giveVotes(eurovision, 20, 23, 817);
    giveVotes(eurovision, 20, 24, 260);
    giveVotes(eurovision, 20, 25, 874);
    giveVotes(eurovision, 20, 26, 501);
    giveVotes(eurovision, 20, 27, 121);
    giveVotes(eurovision, 20, 28, 293);
    giveVotes(eurovision, 20, 29, 710);
    giveVotes(eurovision, 20, 30, 41);
    giveVotes(eurovision, 20, 31, 341);
    giveVotes(eurovision, 20, 32, 591);
    giveVotes(eurovision, 20, 33, 482);
    giveVotes(eurovision, 20, 34, 557);
    giveVotes(eurovision, 20, 35, 489);
    giveVotes(eurovision, 20, 36, 361);
    giveVotes(eurovision, 20, 37, 770);
    giveVotes(eurovision, 20, 38, 348);
    giveVotes(eurovision, 20, 39, 164);
    giveVotes(eurovision, 20, 40, 666);
    giveVotes(eurovision, 20, 41, 441);
    giveVotes(eurovision, 20, 42, 120);
    giveVotes(eurovision, 20, 43, 797);

    giveVotes(eurovision, 21, 1, 320);
    giveVotes(eurovision, 21, 2, 377);
    giveVotes(eurovision, 21, 3, 365);
    giveVotes(eurovision, 21, 4, 26);
    giveVotes(eurovision, 21, 5, 850);
    giveVotes(eurovision, 21, 6, 413);
    giveVotes(eurovision, 21, 7, 920);
    giveVotes(eurovision, 21, 8, 174);
    giveVotes(eurovision, 21, 9, 217);
    giveVotes(eurovision, 21, 10, 418);
    giveVotes(eurovision, 21, 11, 819);
    giveVotes(eurovision, 21, 12, 603);
    giveVotes(eurovision, 21, 13, 279);
    giveVotes(eurovision, 21, 14, 973);
    giveVotes(eurovision, 21, 15, 978);
    giveVotes(eurovision, 21, 16, 441);
    giveVotes(eurovision, 21, 17, 428);
    giveVotes(eurovision, 21, 18, 363);
    giveVotes(eurovision, 21, 19, 830);
    giveVotes(eurovision, 21, 20, 564);
    giveVotes(eurovision, 21, 21, 0);
    giveVotes(eurovision, 21, 22, 977);
    giveVotes(eurovision, 21, 23, 900);
    giveVotes(eurovision, 21, 24, 770);
    giveVotes(eurovision, 21, 25, 963);
    giveVotes(eurovision, 21, 26, 940);
    giveVotes(eurovision, 21, 27, 394);
    giveVotes(eurovision, 21, 28, 446);
    giveVotes(eurovision, 21, 29, 85);
    giveVotes(eurovision, 21, 30, 614);
    giveVotes(eurovision, 21, 31, 285);
    giveVotes(eurovision, 21, 32, 265);
    giveVotes(eurovision, 21, 33, 818);
    giveVotes(eurovision, 21, 34, 89);
    giveVotes(eurovision, 21, 35, 181);
    giveVotes(eurovision, 21, 36, 181);
    giveVotes(eurovision, 21, 37, 620);
    giveVotes(eurovision, 21, 38, 733);
    giveVotes(eurovision, 21, 39, 467);
    giveVotes(eurovision, 21, 40, 197);
    giveVotes(eurovision, 21, 41, 90);
    giveVotes(eurovision, 21, 42, 208);
    giveVotes(eurovision, 21, 43, 237);

    giveVotes(eurovision, 22, 1, 805);
    giveVotes(eurovision, 22, 2, 206);
    giveVotes(eurovision, 22, 3, 609);
    giveVotes(eurovision, 22, 4, 188);
    giveVotes(eurovision, 22, 5, 691);
    giveVotes(eurovision, 22, 6, 845);
    giveVotes(eurovision, 22, 7, 646);
    giveVotes(eurovision, 22, 8, 647);
    giveVotes(eurovision, 22, 9, 21);
    giveVotes(eurovision, 22, 10, 500);
    giveVotes(eurovision, 22, 11, 739);
    giveVotes(eurovision, 22, 12, 87);
    giveVotes(eurovision, 22, 13, 787);
    giveVotes(eurovision, 22, 14, 941);
    giveVotes(eurovision, 22, 15, 757);
    giveVotes(eurovision, 22, 16, 468);
    giveVotes(eurovision, 22, 17, 5);
    giveVotes(eurovision, 22, 18, 143);
    giveVotes(eurovision, 22, 19, 865);
    giveVotes(eurovision, 22, 20, 416);
    giveVotes(eurovision, 22, 21, 479);
    giveVotes(eurovision, 22, 22, 0);
    giveVotes(eurovision, 22, 23, 492);
    giveVotes(eurovision, 22, 24, 434);
    giveVotes(eurovision, 22, 25, 728);
    giveVotes(eurovision, 22, 26, 760);
    giveVotes(eurovision, 22, 27, 481);
    giveVotes(eurovision, 22, 28, 348);
    giveVotes(eurovision, 22, 29, 396);
    giveVotes(eurovision, 22, 30, 321);
    giveVotes(eurovision, 22, 31, 281);
    giveVotes(eurovision, 22, 32, 620);
    giveVotes(eurovision, 22, 33, 19);
    giveVotes(eurovision, 22, 34, 461);
    giveVotes(eurovision, 22, 35, 249);
    giveVotes(eurovision, 22, 36, 119);
    giveVotes(eurovision, 22, 37, 213);
    giveVotes(eurovision, 22, 38, 140);
    giveVotes(eurovision, 22, 39, 548);
    giveVotes(eurovision, 22, 40, 477);
    giveVotes(eurovision, 22, 41, 61);
    giveVotes(eurovision, 22, 42, 480);
    giveVotes(eurovision, 22, 43, 156);

    giveVotes(eurovision, 23, 1, 583);
    giveVotes(eurovision, 23, 2, 480);
    giveVotes(eurovision, 23, 3, 643);
    giveVotes(eurovision, 23, 4, 298);
    giveVotes(eurovision, 23, 5, 822);
    giveVotes(eurovision, 23, 6, 504);
    giveVotes(eurovision, 23, 7, 746);
    giveVotes(eurovision, 23, 8, 663);
    giveVotes(eurovision, 23, 9, 343);
    giveVotes(eurovision, 23, 10, 423);
    giveVotes(eurovision, 23, 11, 970);
    giveVotes(eurovision, 23, 12, 636);
    giveVotes(eurovision, 23, 13, 645);
    giveVotes(eurovision, 23, 14, 846);
    giveVotes(eurovision, 23, 15, 930);
    giveVotes(eurovision, 23, 16, 179);
    giveVotes(eurovision, 23, 17, 547);
    giveVotes(eurovision, 23, 18, 609);
    giveVotes(eurovision, 23, 19, 388);
    giveVotes(eurovision, 23, 20, 214);
    giveVotes(eurovision, 23, 21, 788);
    giveVotes(eurovision, 23, 22, 945);
    giveVotes(eurovision, 23, 23, 0);
    giveVotes(eurovision, 23, 24, 831);
    giveVotes(eurovision, 23, 25, 834);
    giveVotes(eurovision, 23, 26, 915);
    giveVotes(eurovision, 23, 27, 644);
    giveVotes(eurovision, 23, 28, 352);
    giveVotes(eurovision, 23, 29, 705);
    giveVotes(eurovision, 23, 30, 386);
    giveVotes(eurovision, 23, 31, 587);
    giveVotes(eurovision, 23, 32, 293);
    giveVotes(eurovision, 23, 33, 299);
    giveVotes(eurovision, 23, 34, 260);
    giveVotes(eurovision, 23, 35, 686);
    giveVotes(eurovision, 23, 36, 604);
    giveVotes(eurovision, 23, 37, 330);
    giveVotes(eurovision, 23, 38, 191);
    giveVotes(eurovision, 23, 39, 362);
    giveVotes(eurovision, 23, 40, 604);
    giveVotes(eurovision, 23, 41, 427);
    giveVotes(eurovision, 23, 42, 496);
    giveVotes(eurovision, 23, 43, 170);

    giveVotes(eurovision, 24, 1, 575);
    giveVotes(eurovision, 24, 2, 245);
    giveVotes(eurovision, 24, 3, 188);
    giveVotes(eurovision, 24, 4, 980);
    giveVotes(eurovision, 24, 5, 250);
    giveVotes(eurovision, 24, 6, 801);
    giveVotes(eurovision, 24, 7, 537);
    giveVotes(eurovision, 24, 8, 354);
    giveVotes(eurovision, 24, 9, 919);
    giveVotes(eurovision, 24, 10, 499);
    giveVotes(eurovision, 24, 11, 917);
    giveVotes(eurovision, 24, 12, 453);
    giveVotes(eurovision, 24, 13, 580);
    giveVotes(eurovision, 24, 14, 887);
    giveVotes(eurovision, 24, 15, 749);
    giveVotes(eurovision, 24, 16, 397);
    giveVotes(eurovision, 24, 17, 470);
    giveVotes(eurovision, 24, 18, 581);
    giveVotes(eurovision, 24, 19, 705);
    giveVotes(eurovision, 24, 20, 532);
    giveVotes(eurovision, 24, 21, 2);
    giveVotes(eurovision, 24, 22, 868);
    giveVotes(eurovision, 24, 23, 782);
    giveVotes(eurovision, 24, 24, 0);
    giveVotes(eurovision, 24, 25, 818);
    giveVotes(eurovision, 24, 26, 137);
    giveVotes(eurovision, 24, 27, 134);
    giveVotes(eurovision, 24, 28, 297);
    giveVotes(eurovision, 24, 29, 397);
    giveVotes(eurovision, 24, 30, 466);
    giveVotes(eurovision, 24, 31, 430);
    giveVotes(eurovision, 24, 32, 393);
    giveVotes(eurovision, 24, 33, 351);
    giveVotes(eurovision, 24, 34, 521);
    giveVotes(eurovision, 24, 35, 371);
    giveVotes(eurovision, 24, 36, 129);
    giveVotes(eurovision, 24, 37, 145);
    giveVotes(eurovision, 24, 38, 569);
    giveVotes(eurovision, 24, 39, 448);
    giveVotes(eurovision, 24, 40, 562);
    giveVotes(eurovision, 24, 41, 309);
    giveVotes(eurovision, 24, 42, 333);
    giveVotes(eurovision, 24, 43, 540);

    giveVotes(eurovision, 25, 1, 734);
    giveVotes(eurovision, 25, 2, 439);
    giveVotes(eurovision, 25, 3, 355);
    giveVotes(eurovision, 25, 4, 858);
    giveVotes(eurovision, 25, 5, 138);
    giveVotes(eurovision, 25, 6, 838);
    giveVotes(eurovision, 25, 7, 841);
    giveVotes(eurovision, 25, 8, 85);
    giveVotes(eurovision, 25, 9, 139);
    giveVotes(eurovision, 25, 10, 92);
    giveVotes(eurovision, 25, 11, 831);
    giveVotes(eurovision, 25, 12, 92);
    giveVotes(eurovision, 25, 13, 715);
    giveVotes(eurovision, 25, 14, 883);
    giveVotes(eurovision, 25, 15, 528);
    giveVotes(eurovision, 25, 16, 459);
    giveVotes(eurovision, 25, 17, 690);
    giveVotes(eurovision, 25, 18, 986);
    giveVotes(eurovision, 25, 19, 551);
    giveVotes(eurovision, 25, 20, 480);
    giveVotes(eurovision, 25, 21, 539);
    giveVotes(eurovision, 25, 22, 941);
    giveVotes(eurovision, 25, 23, 531);
    giveVotes(eurovision, 25, 24, 562);
    giveVotes(eurovision, 25, 25, 0);
    giveVotes(eurovision, 25, 26, 871);
    giveVotes(eurovision, 25, 27, 535);
    giveVotes(eurovision, 25, 28, 430);
    giveVotes(eurovision, 25, 29, 565);
    giveVotes(eurovision, 25, 30, 474);
    giveVotes(eurovision, 25, 31, 109);
    giveVotes(eurovision, 25, 32, 429);
    giveVotes(eurovision, 25, 33, 621);
    giveVotes(eurovision, 25, 34, 110);
    giveVotes(eurovision, 25, 35, 95);
    giveVotes(eurovision, 25, 36, 81);
    giveVotes(eurovision, 25, 37, 185);
    giveVotes(eurovision, 25, 38, 494);
    giveVotes(eurovision, 25, 39, 431);
    giveVotes(eurovision, 25, 40, 517);
    giveVotes(eurovision, 25, 41, 579);
    giveVotes(eurovision, 25, 42, 343);
    giveVotes(eurovision, 25, 43, 546);

    giveVotes(eurovision, 26, 1, 948);
    giveVotes(eurovision, 26, 2, 603);
    giveVotes(eurovision, 26, 3, 515);
    giveVotes(eurovision, 26, 4, 79);
    giveVotes(eurovision, 26, 5, 4);
    giveVotes(eurovision, 26, 6, 878);
    giveVotes(eurovision, 26, 7, 613);
    giveVotes(eurovision, 26, 8, 633);
    giveVotes(eurovision, 26, 9, 246);
    giveVotes(eurovision, 26, 10, 762);
    giveVotes(eurovision, 26, 11, 799);
    giveVotes(eurovision, 26, 12, 974);
    giveVotes(eurovision, 26, 13, 676);
    giveVotes(eurovision, 26, 14, 494);
    giveVotes(eurovision, 26, 15, 850);
    giveVotes(eurovision, 26, 16, 57);
    giveVotes(eurovision, 26, 17, 0);
    giveVotes(eurovision, 26, 18, 369);
    giveVotes(eurovision, 26, 19, 950);
    giveVotes(eurovision, 26, 20, 489);
    giveVotes(eurovision, 26, 21, 771);
    giveVotes(eurovision, 26, 22, 923);
    giveVotes(eurovision, 26, 23, 21);
    giveVotes(eurovision, 26, 24, 640);
    giveVotes(eurovision, 26, 25, 856);
    giveVotes(eurovision, 26, 26, 0);
    giveVotes(eurovision, 26, 27, 213);
    giveVotes(eurovision, 26, 28, 671);
    giveVotes(eurovision, 26, 29, 39);
    giveVotes(eurovision, 26, 30, 348);
    giveVotes(eurovision, 26, 31, 488);
    giveVotes(eurovision, 26, 32, 121);
    giveVotes(eurovision, 26, 33, 91);
    giveVotes(eurovision, 26, 34, 592);
    giveVotes(eurovision, 26, 35, 428);
    giveVotes(eurovision, 26, 36, 191);
    giveVotes(eurovision, 26, 37, 534);
    giveVotes(eurovision, 26, 38, 260);
    giveVotes(eurovision, 26, 39, 365);
    giveVotes(eurovision, 26, 40, 382);
    giveVotes(eurovision, 26, 41, 116);
    giveVotes(eurovision, 26, 42, 620);
    giveVotes(eurovision, 26, 43, 518);

    giveVotes(eurovision, 27, 1, 970);
    giveVotes(eurovision, 27, 2, 24);
    giveVotes(eurovision, 27, 3, 184);
    giveVotes(eurovision, 27, 4, 726);
    giveVotes(eurovision, 27, 5, 556);
    giveVotes(eurovision, 27, 6, 200);
    giveVotes(eurovision, 27, 7, 967);
    giveVotes(eurovision, 27, 8, 614);
    giveVotes(eurovision, 27, 9, 625);
    giveVotes(eurovision, 27, 10, 537);
    giveVotes(eurovision, 27, 11, 822);
    giveVotes(eurovision, 27, 12, 314);
    giveVotes(eurovision, 27, 13, 280);
    giveVotes(eurovision, 27, 14, 945);
    giveVotes(eurovision, 27, 15, 360);
    giveVotes(eurovision, 27, 16, 487);
    giveVotes(eurovision, 27, 17, 271);
    giveVotes(eurovision, 27, 18, 791);
    giveVotes(eurovision, 27, 19, 862);
    giveVotes(eurovision, 27, 20, 808);
    giveVotes(eurovision, 27, 21, 48);
    giveVotes(eurovision, 27, 22, 997);
    giveVotes(eurovision, 27, 23, 658);
    giveVotes(eurovision, 27, 24, 553);
    giveVotes(eurovision, 27, 25, 973);
    giveVotes(eurovision, 27, 26, 922);
    giveVotes(eurovision, 27, 27, 0);
    giveVotes(eurovision, 27, 28, 247);
    giveVotes(eurovision, 27, 29, 506);
    giveVotes(eurovision, 27, 30, 69);
    giveVotes(eurovision, 27, 31, 323);
    giveVotes(eurovision, 27, 32, 617);
    giveVotes(eurovision, 27, 33, 614);
    giveVotes(eurovision, 27, 34, 150);
    giveVotes(eurovision, 27, 35, 784);
    giveVotes(eurovision, 27, 36, 595);
    giveVotes(eurovision, 27, 37, 720);
    giveVotes(eurovision, 27, 38, 110);
    giveVotes(eurovision, 27, 39, 777);
    giveVotes(eurovision, 27, 40, 439);
    giveVotes(eurovision, 27, 41, 463);
    giveVotes(eurovision, 27, 42, 497);
    giveVotes(eurovision, 27, 43, 498);

    giveVotes(eurovision, 28, 1, 997);
    giveVotes(eurovision, 28, 2, 136);
    giveVotes(eurovision, 28, 3, 182);
    giveVotes(eurovision, 28, 4, 497);
    giveVotes(eurovision, 28, 5, 605);
    giveVotes(eurovision, 28, 6, 353);
    giveVotes(eurovision, 28, 7, 968);
    giveVotes(eurovision, 28, 8, 441);
    giveVotes(eurovision, 28, 9, 31);
    giveVotes(eurovision, 28, 10, 597);
    giveVotes(eurovision, 28, 11, 621);
    giveVotes(eurovision, 28, 12, 283);
    giveVotes(eurovision, 28, 13, 580);
    giveVotes(eurovision, 28, 14, 835);
    giveVotes(eurovision, 28, 15, 923);
    giveVotes(eurovision, 28, 16, 211);
    giveVotes(eurovision, 28, 17, 253);
    giveVotes(eurovision, 28, 18, 796);
    giveVotes(eurovision, 28, 19, 863);
    giveVotes(eurovision, 28, 20, 555);
    giveVotes(eurovision, 28, 21, 802);
    giveVotes(eurovision, 28, 22, 939);
    giveVotes(eurovision, 28, 23, 190);
    giveVotes(eurovision, 28, 24, 580);
    giveVotes(eurovision, 28, 25, 813);
    giveVotes(eurovision, 28, 26, 816);
    giveVotes(eurovision, 28, 27, 476);
    giveVotes(eurovision, 28, 28, 0);
    giveVotes(eurovision, 28, 29, 127);
    giveVotes(eurovision, 28, 30, 219);
    giveVotes(eurovision, 28, 31, 328);
    giveVotes(eurovision, 28, 32, 297);
    giveVotes(eurovision, 28, 33, 504);
    giveVotes(eurovision, 28, 34, 769);
    giveVotes(eurovision, 28, 35, 486);
    giveVotes(eurovision, 28, 36, 122);
    giveVotes(eurovision, 28, 37, 328);
    giveVotes(eurovision, 28, 38, 483);
    giveVotes(eurovision, 28, 39, 468);
    giveVotes(eurovision, 28, 40, 240);
    giveVotes(eurovision, 28, 41, 623);
    giveVotes(eurovision, 28, 42, 593);
    giveVotes(eurovision, 28, 43, 376);

    giveVotes(eurovision, 29, 1, 958);
    giveVotes(eurovision, 29, 2, 112);
    giveVotes(eurovision, 29, 3, 283);
    giveVotes(eurovision, 29, 4, 108);
    giveVotes(eurovision, 29, 5, 474);
    giveVotes(eurovision, 29, 6, 120);
    giveVotes(eurovision, 29, 7, 360);
    giveVotes(eurovision, 29, 8, 651);
    giveVotes(eurovision, 29, 9, 835);
    giveVotes(eurovision, 29, 10, 299);
    giveVotes(eurovision, 29, 11, 909);
    giveVotes(eurovision, 29, 12, 619);
    giveVotes(eurovision, 29, 13, 494);
    giveVotes(eurovision, 29, 14, 983);
    giveVotes(eurovision, 29, 15, 960);
    giveVotes(eurovision, 29, 16, 220);
    giveVotes(eurovision, 29, 17, 349);
    giveVotes(eurovision, 29, 18, 596);
    giveVotes(eurovision, 29, 19, 864);
    giveVotes(eurovision, 29, 20, 484);
    giveVotes(eurovision, 29, 21, 130);
    giveVotes(eurovision, 29, 22, 994);
    giveVotes(eurovision, 29, 23, 213);
    giveVotes(eurovision, 29, 24, 898);
    giveVotes(eurovision, 29, 25, 971);
    giveVotes(eurovision, 29, 26, 980);
    giveVotes(eurovision, 29, 27, 180);
    giveVotes(eurovision, 29, 28, 748);
    giveVotes(eurovision, 29, 29, 0);
    giveVotes(eurovision, 29, 30, 581);
    giveVotes(eurovision, 29, 31, 389);
    giveVotes(eurovision, 29, 32, 425);
    giveVotes(eurovision, 29, 33, 368);
    giveVotes(eurovision, 29, 34, 134);
    giveVotes(eurovision, 29, 35, 8);
    giveVotes(eurovision, 29, 36, 292);
    giveVotes(eurovision, 29, 37, 182);
    giveVotes(eurovision, 29, 38, 780);
    giveVotes(eurovision, 29, 39, 98);
    giveVotes(eurovision, 29, 40, 93);
    giveVotes(eurovision, 29, 41, 466);
    giveVotes(eurovision, 29, 42, 664);
    giveVotes(eurovision, 29, 43, 184);

    giveVotes(eurovision, 30, 1, 835);
    giveVotes(eurovision, 30, 2, 539);
    giveVotes(eurovision, 30, 3, 751);
    giveVotes(eurovision, 30, 4, 667);
    giveVotes(eurovision, 30, 5, 443);
    giveVotes(eurovision, 30, 6, 51);
    giveVotes(eurovision, 30, 7, 52);
    giveVotes(eurovision, 30, 8, 192);
    giveVotes(eurovision, 30, 9, 305);
    giveVotes(eurovision, 30, 10, 936);
    giveVotes(eurovision, 30, 11, 415);
    giveVotes(eurovision, 30, 12, 994);
    giveVotes(eurovision, 30, 13, 655);
    giveVotes(eurovision, 30, 14, 850);
    giveVotes(eurovision, 30, 15, 631);
    giveVotes(eurovision, 30, 16, 658);
    giveVotes(eurovision, 30, 17, 34);
    giveVotes(eurovision, 30, 18, 880);
    giveVotes(eurovision, 30, 19, 749);
    giveVotes(eurovision, 30, 20, 46);
    giveVotes(eurovision, 30, 21, 502);
    giveVotes(eurovision, 30, 22, 813);
    giveVotes(eurovision, 30, 23, 526);
    giveVotes(eurovision, 30, 24, 228);
    giveVotes(eurovision, 30, 25, 910);
    giveVotes(eurovision, 30, 26, 862);
    giveVotes(eurovision, 30, 27, 598);
    giveVotes(eurovision, 30, 28, 504);
    giveVotes(eurovision, 30, 29, 743);
    giveVotes(eurovision, 30, 30, 0);
    giveVotes(eurovision, 30, 31, 559);
    giveVotes(eurovision, 30, 32, 636);
    giveVotes(eurovision, 30, 33, 473);
    giveVotes(eurovision, 30, 34, 355);
    giveVotes(eurovision, 30, 35, 209);
    giveVotes(eurovision, 30, 36, 146);
    giveVotes(eurovision, 30, 37, 602);
    giveVotes(eurovision, 30, 38, 521);
    giveVotes(eurovision, 30, 39, 37);
    giveVotes(eurovision, 30, 40, 507);
    giveVotes(eurovision, 30, 41, 684);
    giveVotes(eurovision, 30, 42, 301);
    giveVotes(eurovision, 30, 43, 505);

    giveVotes(eurovision, 31, 1, 663);
    giveVotes(eurovision, 31, 2, 135);
    giveVotes(eurovision, 31, 3, 85);
    giveVotes(eurovision, 31, 4, 802);
    giveVotes(eurovision, 31, 5, 54);
    giveVotes(eurovision, 31, 6, 280);
    giveVotes(eurovision, 31, 7, 660);
    giveVotes(eurovision, 31, 8, 351);
    giveVotes(eurovision, 31, 9, 707);
    giveVotes(eurovision, 31, 10, 233);
    giveVotes(eurovision, 31, 11, 757);
    giveVotes(eurovision, 31, 12, 516);
    giveVotes(eurovision, 31, 13, 546);
    giveVotes(eurovision, 31, 14, 744);
    giveVotes(eurovision, 31, 15, 719);
    giveVotes(eurovision, 31, 16, 813);
    giveVotes(eurovision, 31, 17, 5);
    giveVotes(eurovision, 31, 18, 898);
    giveVotes(eurovision, 31, 19, 420);
    giveVotes(eurovision, 31, 20, 704);
    giveVotes(eurovision, 31, 21, 474);
    giveVotes(eurovision, 31, 22, 900);
    giveVotes(eurovision, 31, 23, 527);
    giveVotes(eurovision, 31, 24, 3);
    giveVotes(eurovision, 31, 25, 987);
    giveVotes(eurovision, 31, 26, 993);
    giveVotes(eurovision, 31, 27, 47);
    giveVotes(eurovision, 31, 28, 280);
    giveVotes(eurovision, 31, 29, 52);
    giveVotes(eurovision, 31, 30, 130);
    giveVotes(eurovision, 31, 31, 0);
    giveVotes(eurovision, 31, 32, 267);
    giveVotes(eurovision, 31, 33, 638);
    giveVotes(eurovision, 31, 34, 91);
    giveVotes(eurovision, 31, 35, 323);
    giveVotes(eurovision, 31, 36, 599);
    giveVotes(eurovision, 31, 37, 184);
    giveVotes(eurovision, 31, 38, 347);
    giveVotes(eurovision, 31, 39, 175);
    giveVotes(eurovision, 31, 40, 680);
    giveVotes(eurovision, 31, 41, 658);
    giveVotes(eurovision, 31, 42, 100);
    giveVotes(eurovision, 31, 43, 660);

    giveVotes(eurovision, 32, 1, 941);
    giveVotes(eurovision, 32, 2, 727);
    giveVotes(eurovision, 32, 3, 389);
    giveVotes(eurovision, 32, 4, 932);
    giveVotes(eurovision, 32, 5, 808);
    giveVotes(eurovision, 32, 6, 931);
    giveVotes(eurovision, 32, 7, 392);
    giveVotes(eurovision, 32, 8, 672);
    giveVotes(eurovision, 32, 9, 739);
    giveVotes(eurovision, 32, 10, 175);
    giveVotes(eurovision, 32, 11, 515);
    giveVotes(eurovision, 32, 12, 375);
    giveVotes(eurovision, 32, 13, 887);
    giveVotes(eurovision, 32, 14, 207);
    giveVotes(eurovision, 32, 15, 830);
    giveVotes(eurovision, 32, 16, 115);
    giveVotes(eurovision, 32, 17, 247);
    giveVotes(eurovision, 32, 18, 536);
    giveVotes(eurovision, 32, 19, 861);
    giveVotes(eurovision, 32, 20, 607);
    giveVotes(eurovision, 32, 21, 699);
    giveVotes(eurovision, 32, 22, 980);
    giveVotes(eurovision, 32, 23, 67);
    giveVotes(eurovision, 32, 24, 152);
    giveVotes(eurovision, 32, 25, 941);
    giveVotes(eurovision, 32, 26, 889);
    giveVotes(eurovision, 32, 27, 513);
    giveVotes(eurovision, 32, 28, 158);
    giveVotes(eurovision, 32, 29, 679);
    giveVotes(eurovision, 32, 30, 501);
    giveVotes(eurovision, 32, 31, 795);
    giveVotes(eurovision, 32, 32, 0);
    giveVotes(eurovision, 32, 33, 536);
    giveVotes(eurovision, 32, 34, 147);
    giveVotes(eurovision, 32, 35, 132);
    giveVotes(eurovision, 32, 36, 29);
    giveVotes(eurovision, 32, 37, 38);
    giveVotes(eurovision, 32, 38, 56);
    giveVotes(eurovision, 32, 39, 320);
    giveVotes(eurovision, 32, 40, 637);
    giveVotes(eurovision, 32, 41, 136);
    giveVotes(eurovision, 32, 42, 209);
    giveVotes(eurovision, 32, 43, 528);

    giveVotes(eurovision, 33, 1, 871);
    giveVotes(eurovision, 33, 2, 240);
    giveVotes(eurovision, 33, 3, 442);
    giveVotes(eurovision, 33, 4, 998);
    giveVotes(eurovision, 33, 5, 185);
    giveVotes(eurovision, 33, 6, 993);
    giveVotes(eurovision, 33, 7, 867);
    giveVotes(eurovision, 33, 8, 789);
    giveVotes(eurovision, 33, 9, 203);
    giveVotes(eurovision, 33, 10, 714);
    giveVotes(eurovision, 33, 11, 402);
    giveVotes(eurovision, 33, 12, 309);
    giveVotes(eurovision, 33, 13, 753);
    giveVotes(eurovision, 33, 14, 902);
    giveVotes(eurovision, 33, 15, 909);
    giveVotes(eurovision, 33, 16, 490);
    giveVotes(eurovision, 33, 17, 379);
    giveVotes(eurovision, 33, 18, 479);
    giveVotes(eurovision, 33, 19, 983);
    giveVotes(eurovision, 33, 20, 428);
    giveVotes(eurovision, 33, 21, 516);
    giveVotes(eurovision, 33, 22, 987);
    giveVotes(eurovision, 33, 23, 21);
    giveVotes(eurovision, 33, 24, 231);
    giveVotes(eurovision, 33, 25, 851);
    giveVotes(eurovision, 33, 26, 942);
    giveVotes(eurovision, 33, 27, 123);
    giveVotes(eurovision, 33, 28, 732);
    giveVotes(eurovision, 33, 29, 496);
    giveVotes(eurovision, 33, 30, 845);
    giveVotes(eurovision, 33, 31, 104);
    giveVotes(eurovision, 33, 32, 445);
    giveVotes(eurovision, 33, 33, 0);
    giveVotes(eurovision, 33, 34, 763);
    giveVotes(eurovision, 33, 35, 176);
    giveVotes(eurovision, 33, 36, 191);
    giveVotes(eurovision, 33, 37, 77);
    giveVotes(eurovision, 33, 38, 81);
    giveVotes(eurovision, 33, 39, 597);
    giveVotes(eurovision, 33, 40, 399);
    giveVotes(eurovision, 33, 41, 646);
    giveVotes(eurovision, 33, 42, 46);
    giveVotes(eurovision, 33, 43, 289);

    giveVotes(eurovision, 34, 1, 272);
    giveVotes(eurovision, 34, 2, 527);
    giveVotes(eurovision, 34, 3, 878);
    giveVotes(eurovision, 34, 4, 173);
    giveVotes(eurovision, 34, 5, 537);
    giveVotes(eurovision, 34, 6, 566);
    giveVotes(eurovision, 34, 7, 588);
    giveVotes(eurovision, 34, 8, 302);
    giveVotes(eurovision, 34, 9, 563);
    giveVotes(eurovision, 34, 10, 989);
    giveVotes(eurovision, 34, 11, 783);
    giveVotes(eurovision, 34, 12, 841);
    giveVotes(eurovision, 34, 13, 579);
    giveVotes(eurovision, 34, 14, 848);
    giveVotes(eurovision, 34, 15, 767);
    giveVotes(eurovision, 34, 16, 397);
    giveVotes(eurovision, 34, 17, 440);
    giveVotes(eurovision, 34, 18, 751);
    giveVotes(eurovision, 34, 19, 462);
    giveVotes(eurovision, 34, 20, 572);
    giveVotes(eurovision, 34, 21, 451);
    giveVotes(eurovision, 34, 22, 850);
    giveVotes(eurovision, 34, 23, 562);
    giveVotes(eurovision, 34, 24, 575);
    giveVotes(eurovision, 34, 25, 958);
    giveVotes(eurovision, 34, 26, 968);
    giveVotes(eurovision, 34, 27, 411);
    giveVotes(eurovision, 34, 28, 452);
    giveVotes(eurovision, 34, 29, 446);
    giveVotes(eurovision, 34, 30, 685);
    giveVotes(eurovision, 34, 31, 149);
    giveVotes(eurovision, 34, 32, 256);
    giveVotes(eurovision, 34, 33, 542);
    giveVotes(eurovision, 34, 34, 0);
    giveVotes(eurovision, 34, 35, 286);
    giveVotes(eurovision, 34, 36, 743);
    giveVotes(eurovision, 34, 37, 155);
    giveVotes(eurovision, 34, 38, 398);
    giveVotes(eurovision, 34, 39, 188);
    giveVotes(eurovision, 34, 40, 112);
    giveVotes(eurovision, 34, 41, 216);
    giveVotes(eurovision, 34, 42, 105);
    giveVotes(eurovision, 34, 43, 577);

    giveVotes(eurovision, 35, 1, 531);
    giveVotes(eurovision, 35, 2, 508);
    giveVotes(eurovision, 35, 3, 498);
    giveVotes(eurovision, 35, 4, 461);
    giveVotes(eurovision, 35, 5, 114);
    giveVotes(eurovision, 35, 6, 118);
    giveVotes(eurovision, 35, 7, 462);
    giveVotes(eurovision, 35, 8, 476);
    giveVotes(eurovision, 35, 9, 392);
    giveVotes(eurovision, 35, 10, 278);
    giveVotes(eurovision, 35, 11, 756);
    giveVotes(eurovision, 35, 12, 613);
    giveVotes(eurovision, 35, 13, 430);
    giveVotes(eurovision, 35, 14, 699);
    giveVotes(eurovision, 35, 15, 652);
    giveVotes(eurovision, 35, 16, 197);
    giveVotes(eurovision, 35, 17, 23);
    giveVotes(eurovision, 35, 18, 773);
    giveVotes(eurovision, 35, 19, 982);
    giveVotes(eurovision, 35, 20, 203);
    giveVotes(eurovision, 35, 21, 925);
    giveVotes(eurovision, 35, 22, 923);
    giveVotes(eurovision, 35, 23, 578);
    giveVotes(eurovision, 35, 24, 613);
    giveVotes(eurovision, 35, 25, 905);
    giveVotes(eurovision, 35, 26, 857);
    giveVotes(eurovision, 35, 27, 281);
    giveVotes(eurovision, 35, 28, 365);
    giveVotes(eurovision, 35, 29, 539);
    giveVotes(eurovision, 35, 30, 464);
    giveVotes(eurovision, 35, 31, 573);
    giveVotes(eurovision, 35, 32, 598);
    giveVotes(eurovision, 35, 33, 187);
    giveVotes(eurovision, 35, 34, 437);
    giveVotes(eurovision, 35, 35, 0);
    giveVotes(eurovision, 35, 36, 597);
    giveVotes(eurovision, 35, 37, 97);
    giveVotes(eurovision, 35, 38, 96);
    giveVotes(eurovision, 35, 39, 463);
    giveVotes(eurovision, 35, 40, 525);
    giveVotes(eurovision, 35, 41, 137);
    giveVotes(eurovision, 35, 42, 525);
    giveVotes(eurovision, 35, 43, 452);

    giveVotes(eurovision, 36, 1, 564);
    giveVotes(eurovision, 36, 2, 642);
    giveVotes(eurovision, 36, 3, 647);
    giveVotes(eurovision, 36, 4, 245);
    giveVotes(eurovision, 36, 5, 873);
    giveVotes(eurovision, 36, 6, 11);
    giveVotes(eurovision, 36, 7, 868);
    giveVotes(eurovision, 36, 8, 517);
    giveVotes(eurovision, 36, 9, 260);
    giveVotes(eurovision, 36, 10, 229);
    giveVotes(eurovision, 36, 11, 906);
    giveVotes(eurovision, 36, 12, 379);
    giveVotes(eurovision, 36, 13, 882);
    giveVotes(eurovision, 36, 14, 960);
    giveVotes(eurovision, 36, 15, 995);
    giveVotes(eurovision, 36, 16, 475);
    giveVotes(eurovision, 36, 17, 863);
    giveVotes(eurovision, 36, 18, 299);
    giveVotes(eurovision, 36, 19, 161);
    giveVotes(eurovision, 36, 20, 836);
    giveVotes(eurovision, 36, 21, 40);
    giveVotes(eurovision, 36, 22, 884);
    giveVotes(eurovision, 36, 23, 459);
    giveVotes(eurovision, 36, 24, 74);
    giveVotes(eurovision, 36, 25, 788);
    giveVotes(eurovision, 36, 26, 757);
    giveVotes(eurovision, 36, 27, 320);
    giveVotes(eurovision, 36, 28, 108);
    giveVotes(eurovision, 36, 29, 405);
    giveVotes(eurovision, 36, 30, 239);
    giveVotes(eurovision, 36, 31, 697);
    giveVotes(eurovision, 36, 32, 360);
    giveVotes(eurovision, 36, 33, 265);
    giveVotes(eurovision, 36, 34, 236);
    giveVotes(eurovision, 36, 35, 132);
    giveVotes(eurovision, 36, 36, 0);
    giveVotes(eurovision, 36, 37, 662);
    giveVotes(eurovision, 36, 38, 751);
    giveVotes(eurovision, 36, 39, 139);
    giveVotes(eurovision, 36, 40, 520);
    giveVotes(eurovision, 36, 41, 648);
    giveVotes(eurovision, 36, 42, 455);
    giveVotes(eurovision, 36, 43, 589);

    giveVotes(eurovision, 37, 1, 24);
    giveVotes(eurovision, 37, 2, 676);
    giveVotes(eurovision, 37, 3, 537);
    giveVotes(eurovision, 37, 4, 435);
    giveVotes(eurovision, 37, 5, 773);
    giveVotes(eurovision, 37, 6, 627);
    giveVotes(eurovision, 37, 7, 174);
    giveVotes(eurovision, 37, 8, 270);
    giveVotes(eurovision, 37, 9, 376);
    giveVotes(eurovision, 37, 10, 425);
    giveVotes(eurovision, 37, 11, 697);
    giveVotes(eurovision, 37, 12, 215);
    giveVotes(eurovision, 37, 13, 929);
    giveVotes(eurovision, 37, 14, 681);
    giveVotes(eurovision, 37, 15, 842);
    giveVotes(eurovision, 37, 16, 676);
    giveVotes(eurovision, 37, 17, 251);
    giveVotes(eurovision, 37, 18, 477);
    giveVotes(eurovision, 37, 19, 641);
    giveVotes(eurovision, 37, 20, 244);
    giveVotes(eurovision, 37, 21, 504);
    giveVotes(eurovision, 37, 22, 951);
    giveVotes(eurovision, 37, 23, 981);
    giveVotes(eurovision, 37, 24, 803);
    giveVotes(eurovision, 37, 25, 902);
    giveVotes(eurovision, 37, 26, 888);
    giveVotes(eurovision, 37, 27, 237);
    giveVotes(eurovision, 37, 28, 343);
    giveVotes(eurovision, 37, 29, 648);
    giveVotes(eurovision, 37, 30, 214);
    giveVotes(eurovision, 37, 31, 532);
    giveVotes(eurovision, 37, 32, 131);
    giveVotes(eurovision, 37, 33, 220);
    giveVotes(eurovision, 37, 34, 361);
    giveVotes(eurovision, 37, 35, 667);
    giveVotes(eurovision, 37, 36, 320);
    giveVotes(eurovision, 37, 37, 0);
    giveVotes(eurovision, 37, 38, 522);
    giveVotes(eurovision, 37, 39, 554);
    giveVotes(eurovision, 37, 40, 27);
    giveVotes(eurovision, 37, 41, 542);
    giveVotes(eurovision, 37, 42, 106);
    giveVotes(eurovision, 37, 43, 653);

    giveVotes(eurovision, 38, 1, 944);
    giveVotes(eurovision, 38, 2, 351);
    giveVotes(eurovision, 38, 3, 121);
    giveVotes(eurovision, 38, 4, 337);
    giveVotes(eurovision, 38, 5, 66);
    giveVotes(eurovision, 38, 6, 507);
    giveVotes(eurovision, 38, 7, 255);
    giveVotes(eurovision, 38, 8, 552);
    giveVotes(eurovision, 38, 9, 309);
    giveVotes(eurovision, 38, 10, 723);
    giveVotes(eurovision, 38, 11, 782);
    giveVotes(eurovision, 38, 12, 387);
    giveVotes(eurovision, 38, 13, 812);
    giveVotes(eurovision, 38, 14, 826);
    giveVotes(eurovision, 38, 15, 833);
    giveVotes(eurovision, 38, 16, 718);
    giveVotes(eurovision, 38, 17, 269);
    giveVotes(eurovision, 38, 18, 688);
    giveVotes(eurovision, 38, 19, 775);
    giveVotes(eurovision, 38, 20, 700);
    giveVotes(eurovision, 38, 21, 853);
    giveVotes(eurovision, 38, 22, 932);
    giveVotes(eurovision, 38, 23, 110);
    giveVotes(eurovision, 38, 24, 785);
    giveVotes(eurovision, 38, 25, 869);
    giveVotes(eurovision, 38, 26, 831);
    giveVotes(eurovision, 38, 27, 681);
    giveVotes(eurovision, 38, 28, 768);
    giveVotes(eurovision, 38, 29, 484);
    giveVotes(eurovision, 38, 30, 15);
    giveVotes(eurovision, 38, 31, 625);
    giveVotes(eurovision, 38, 32, 679);
    giveVotes(eurovision, 38, 33, 322);
    giveVotes(eurovision, 38, 34, 552);
    giveVotes(eurovision, 38, 35, 214);
    giveVotes(eurovision, 38, 36, 70);
    giveVotes(eurovision, 38, 37, 375);
    giveVotes(eurovision, 38, 38, 0);
    giveVotes(eurovision, 38, 39, 268);
    giveVotes(eurovision, 38, 40, 173);
    giveVotes(eurovision, 38, 41, 6);
    giveVotes(eurovision, 38, 42, 193);
    giveVotes(eurovision, 38, 43, 241);

    giveVotes(eurovision, 39, 1, 860);
    giveVotes(eurovision, 39, 2, 60);
    giveVotes(eurovision, 39, 3, 365);
    giveVotes(eurovision, 39, 4, 299);
    giveVotes(eurovision, 39, 5, 895);
    giveVotes(eurovision, 39, 6, 832);
    giveVotes(eurovision, 39, 7, 906);
    giveVotes(eurovision, 39, 8, 356);
    giveVotes(eurovision, 39, 9, 558);
    giveVotes(eurovision, 39, 10, 516);
    giveVotes(eurovision, 39, 11, 809);
    giveVotes(eurovision, 39, 12, 559);
    giveVotes(eurovision, 39, 13, 870);
    giveVotes(eurovision, 39, 14, 915);
    giveVotes(eurovision, 39, 15, 683);
    giveVotes(eurovision, 39, 16, 251);
    giveVotes(eurovision, 39, 17, 101);
    giveVotes(eurovision, 39, 18, 445);
    giveVotes(eurovision, 39, 19, 812);
    giveVotes(eurovision, 39, 20, 502);
    giveVotes(eurovision, 39, 21, 109);
    giveVotes(eurovision, 39, 22, 958);
    giveVotes(eurovision, 39, 23, 462);
    giveVotes(eurovision, 39, 24, 561);
    giveVotes(eurovision, 39, 25, 972);
    giveVotes(eurovision, 39, 26, 841);
    giveVotes(eurovision, 39, 27, 355);
    giveVotes(eurovision, 39, 28, 105);
    giveVotes(eurovision, 39, 29, 584);
    giveVotes(eurovision, 39, 30, 294);
    giveVotes(eurovision, 39, 31, 286);
    giveVotes(eurovision, 39, 32, 319);
    giveVotes(eurovision, 39, 33, 286);
    giveVotes(eurovision, 39, 34, 758);
    giveVotes(eurovision, 39, 35, 164);
    giveVotes(eurovision, 39, 36, 118);
    giveVotes(eurovision, 39, 37, 348);
    giveVotes(eurovision, 39, 38, 88);
    giveVotes(eurovision, 39, 39, 0);
    giveVotes(eurovision, 39, 40, 14);
    giveVotes(eurovision, 39, 41, 524);
    giveVotes(eurovision, 39, 42, 768);
    giveVotes(eurovision, 39, 43, 316);

    giveVotes(eurovision, 40, 1, 63);
    giveVotes(eurovision, 40, 2, 490);
    giveVotes(eurovision, 40, 3, 233);
    giveVotes(eurovision, 40, 4, 689);
    giveVotes(eurovision, 40, 5, 107);
    giveVotes(eurovision, 40, 6, 765);
    giveVotes(eurovision, 40, 7, 763);
    giveVotes(eurovision, 40, 8, 440);
    giveVotes(eurovision, 40, 9, 164);
    giveVotes(eurovision, 40, 10, 382);
    giveVotes(eurovision, 40, 11, 725);
    giveVotes(eurovision, 40, 12, 884);
    giveVotes(eurovision, 40, 13, 273);
    giveVotes(eurovision, 40, 14, 782);
    giveVotes(eurovision, 40, 15, 419);
    giveVotes(eurovision, 40, 16, 449);
    giveVotes(eurovision, 40, 17, 639);
    giveVotes(eurovision, 40, 18, 770);
    giveVotes(eurovision, 40, 19, 721);
    giveVotes(eurovision, 40, 20, 658);
    giveVotes(eurovision, 40, 21, 497);
    giveVotes(eurovision, 40, 22, 778);
    giveVotes(eurovision, 40, 23, 699);
    giveVotes(eurovision, 40, 24, 701);
    giveVotes(eurovision, 40, 25, 885);
    giveVotes(eurovision, 40, 26, 870);
    giveVotes(eurovision, 40, 27, 261);
    giveVotes(eurovision, 40, 28, 26);
    giveVotes(eurovision, 40, 29, 139);
    giveVotes(eurovision, 40, 30, 43);
    giveVotes(eurovision, 40, 31, 427);
    giveVotes(eurovision, 40, 32, 670);
    giveVotes(eurovision, 40, 33, 114);
    giveVotes(eurovision, 40, 34, 180);
    giveVotes(eurovision, 40, 35, 257);
    giveVotes(eurovision, 40, 36, 509);
    giveVotes(eurovision, 40, 37, 312);
    giveVotes(eurovision, 40, 38, 468);
    giveVotes(eurovision, 40, 39, 58);
    giveVotes(eurovision, 40, 40, 0);
    giveVotes(eurovision, 40, 41, 35);
    giveVotes(eurovision, 40, 42, 494);
    giveVotes(eurovision, 40, 43, 654);

    giveVotes(eurovision, 41, 1, 904);
    giveVotes(eurovision, 41, 2, 604);
    giveVotes(eurovision, 41, 3, 845);
    giveVotes(eurovision, 41, 4, 134);
    giveVotes(eurovision, 41, 5, 15);
    giveVotes(eurovision, 41, 6, 334);
    giveVotes(eurovision, 41, 7, 431);
    giveVotes(eurovision, 41, 8, 18);
    giveVotes(eurovision, 41, 9, 492);
    giveVotes(eurovision, 41, 10, 989);
    giveVotes(eurovision, 41, 11, 471);
    giveVotes(eurovision, 41, 12, 951);
    giveVotes(eurovision, 41, 13, 46);
    giveVotes(eurovision, 41, 14, 162);
    giveVotes(eurovision, 41, 15, 696);
    giveVotes(eurovision, 41, 16, 483);
    giveVotes(eurovision, 41, 17, 699);
    giveVotes(eurovision, 41, 18, 828);
    giveVotes(eurovision, 41, 19, 399);
    giveVotes(eurovision, 41, 20, 814);
    giveVotes(eurovision, 41, 21, 825);
    giveVotes(eurovision, 41, 22, 791);
    giveVotes(eurovision, 41, 23, 589);
    giveVotes(eurovision, 41, 24, 190);
    giveVotes(eurovision, 41, 25, 839);
    giveVotes(eurovision, 41, 26, 941);
    giveVotes(eurovision, 41, 27, 257);
    giveVotes(eurovision, 41, 28, 785);
    giveVotes(eurovision, 41, 29, 149);
    giveVotes(eurovision, 41, 30, 522);
    giveVotes(eurovision, 41, 31, 500);
    giveVotes(eurovision, 41, 32, 731);
    giveVotes(eurovision, 41, 33, 697);
    giveVotes(eurovision, 41, 34, 163);
    giveVotes(eurovision, 41, 35, 599);
    giveVotes(eurovision, 41, 36, 419);
    giveVotes(eurovision, 41, 37, 0);
    giveVotes(eurovision, 41, 38, 762);
    giveVotes(eurovision, 41, 39, 136);
    giveVotes(eurovision, 41, 40, 99);
    giveVotes(eurovision, 41, 41, 0);
    giveVotes(eurovision, 41, 42, 656);
    giveVotes(eurovision, 41, 43, 218);

    giveVotes(eurovision, 42, 1, 974);
    giveVotes(eurovision, 42, 2, 129);
    giveVotes(eurovision, 42, 3, 767);
    giveVotes(eurovision, 42, 4, 68);
    giveVotes(eurovision, 42, 5, 273);
    giveVotes(eurovision, 42, 6, 856);
    giveVotes(eurovision, 42, 7, 958);
    giveVotes(eurovision, 42, 8, 213);
    giveVotes(eurovision, 42, 9, 704);
    giveVotes(eurovision, 42, 10, 470);
    giveVotes(eurovision, 42, 11, 117);
    giveVotes(eurovision, 42, 12, 587);
    giveVotes(eurovision, 42, 13, 731);
    giveVotes(eurovision, 42, 14, 852);
    giveVotes(eurovision, 42, 15, 962);
    giveVotes(eurovision, 42, 16, 17);
    giveVotes(eurovision, 42, 17, 448);
    giveVotes(eurovision, 42, 18, 722);
    giveVotes(eurovision, 42, 19, 978);
    giveVotes(eurovision, 42, 20, 822);
    giveVotes(eurovision, 42, 21, 496);
    giveVotes(eurovision, 42, 22, 974);
    giveVotes(eurovision, 42, 23, 7);
    giveVotes(eurovision, 42, 24, 747);
    giveVotes(eurovision, 42, 25, 875);
    giveVotes(eurovision, 42, 26, 960);
    giveVotes(eurovision, 42, 27, 18);
    giveVotes(eurovision, 42, 28, 786);
    giveVotes(eurovision, 42, 29, 810);
    giveVotes(eurovision, 42, 30, 775);
    giveVotes(eurovision, 42, 31, 549);
    giveVotes(eurovision, 42, 32, 417);
    giveVotes(eurovision, 42, 33, 458);
    giveVotes(eurovision, 42, 34, 361);
    giveVotes(eurovision, 42, 35, 230);
    giveVotes(eurovision, 42, 36, 335);
    giveVotes(eurovision, 42, 37, 147);
    giveVotes(eurovision, 42, 38, 587);
    giveVotes(eurovision, 42, 39, 339);
    giveVotes(eurovision, 42, 40, 762);
    giveVotes(eurovision, 42, 41, 715);
    giveVotes(eurovision, 42, 42, 0);
    giveVotes(eurovision, 42, 43, 130);

    giveVotes(eurovision, 43, 1, 6);
    giveVotes(eurovision, 43, 2, 701);
    giveVotes(eurovision, 43, 3, 471);
    giveVotes(eurovision, 43, 4, 498);
    giveVotes(eurovision, 43, 5, 769);
    giveVotes(eurovision, 43, 6, 180);
    giveVotes(eurovision, 43, 7, 592);
    giveVotes(eurovision, 43, 8, 975);
    giveVotes(eurovision, 43, 9, 514);
    giveVotes(eurovision, 43, 10, 992);
    giveVotes(eurovision, 43, 11, 881);
    giveVotes(eurovision, 43, 12, 945);
    giveVotes(eurovision, 43, 13, 373);
    giveVotes(eurovision, 43, 14, 379);
    giveVotes(eurovision, 43, 15, 708);
    giveVotes(eurovision, 43, 16, 303);
    giveVotes(eurovision, 43, 17, 436);
    giveVotes(eurovision, 43, 18, 467);
    giveVotes(eurovision, 43, 19, 573);
    giveVotes(eurovision, 43, 20, 100);
    giveVotes(eurovision, 43, 21, 28);
    giveVotes(eurovision, 43, 22, 770);
    giveVotes(eurovision, 43, 23, 556);
    giveVotes(eurovision, 43, 24, 220);
    giveVotes(eurovision, 43, 25, 716);
    giveVotes(eurovision, 43, 26, 957);
    giveVotes(eurovision, 43, 27, 541);
    giveVotes(eurovision, 43, 28, 227);
    giveVotes(eurovision, 43, 29, 170);
    giveVotes(eurovision, 43, 30, 678);
    giveVotes(eurovision, 43, 31, 283);
    giveVotes(eurovision, 43, 32, 26);
    giveVotes(eurovision, 43, 33, 276);
    giveVotes(eurovision, 43, 34, 192);
    giveVotes(eurovision, 43, 35, 681);
    giveVotes(eurovision, 43, 36, 44);
    giveVotes(eurovision, 43, 37, 377);
    giveVotes(eurovision, 43, 38, 25);
    giveVotes(eurovision, 43, 39, 133);
    giveVotes(eurovision, 43, 40, 323);
    giveVotes(eurovision, 43, 41, 593);
    giveVotes(eurovision, 43, 42, 355);
    giveVotes(eurovision, 43, 43, 0);
}

bool testAddState() {
    Eurovision eurovision = setupEurovision();
    CHECK(eurovisionAddState(eurovision, 0, "israel", "home"), EUROVISION_SUCCESS);
    CHECK(eurovisionAddState(eurovision, 1, "malta", "chameleon"), EUROVISION_SUCCESS);
    CHECK(eurovisionAddState(eurovision, 0, "croatia", "the dream"), EUROVISION_STATE_ALREADY_EXIST);
    CHECK(eurovisionAddState(eurovision, 0, "israel", "home"), EUROVISION_STATE_ALREADY_EXIST);
    CHECK(eurovisionAddState(eurovision, -1, "croatia", "the dream"), EUROVISION_INVALID_ID);
    eurovisionDestroy(eurovision);
    return true;
}

bool testRemoveState() {
    int *results;
    Eurovision eurovision = setupEurovision();
    setupEurovisionStates(eurovision);
    CHECK(eurovisionRemoveState(eurovision, 44), EUROVISION_STATE_NOT_EXIST);
    CHECK(eurovisionRemoveState(eurovision, -1), EUROVISION_INVALID_ID);
    CHECK(eurovisionRemoveState(eurovision, 1), EUROVISION_SUCCESS);
    CHECK(eurovisionRemoveState(eurovision, 1), EUROVISION_STATE_NOT_EXIST);
    results = makeJudgeResults(14, 13, 10, 8, 4, 5, 6, 15, 22, 2);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 0, "piere", results), EUROVISION_SUCCESS, results);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 0, "piere", results), EUROVISION_JUDGE_ALREADY_EXIST, results);
    eurovisionRemoveState(eurovision, 8);
    free(results);
    results = makeJudgeResults(14, 13, 10, 9, 4, 5, 6, 15, 22, 2);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 0, "piere", results), EUROVISION_SUCCESS, results);
    free(results);
    eurovisionDestroy(eurovision);
    return true;
}

bool testAddJudge() {
    int *results;
    Eurovision eurovision = setupEurovision();
    setupEurovisionStates(eurovision);
    CHECK(eurovisionAddJudge(eurovision, -1, "olsen", NULL), EUROVISION_NULL_ARGUMENT);
    results = makeJudgeResults(10, 1, 2, 3, 4, 5, 6, 7, 8, 9);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, -1, "olsen", results), EUROVISION_INVALID_ID, results);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 0, "olsen", results), EUROVISION_SUCCESS, results);
    free(results);
    results = makeJudgeResults(10, 1, -2, -3, 4, 5, 6, 7, 8, -9);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 1, "tanel", results), EUROVISION_INVALID_ID, results);
    free(results);
    results = makeJudgeResults(100, 1, 2, 3, 4, 5, 6, 7, 8, 9);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 1, "tanel", results), EUROVISION_STATE_NOT_EXIST, results);
    free(results);
    results = makeJudgeResults(14, 13, 10, 8, 4, 5, 6, 15, 22, 2);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 1, "tanel", results), EUROVISION_SUCCESS, results);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 1, "marie", results), EUROVISION_JUDGE_ALREADY_EXIST, results);
    CHECK_WITH_FREE(eurovisionAddJudge(eurovision, 2, "marie", results), EUROVISION_SUCCESS, results);
    free(results);
    eurovisionDestroy(eurovision);
    return true;
}

bool testRemoveJudge() {
    Eurovision eurovision = setupEurovision();
    setupEurovisionStates(eurovision);
    setupEurovisionJudges(eurovision);
    CHECK(eurovisionRemoveJudge(eurovision, -1), EUROVISION_INVALID_ID);
    CHECK(eurovisionRemoveJudge(eurovision, 44), EUROVISION_JUDGE_NOT_EXIST);
    CHECK(eurovisionRemoveJudge(eurovision, 3), EUROVISION_SUCCESS);
    CHECK(eurovisionRemoveJudge(eurovision, 3), EUROVISION_JUDGE_NOT_EXIST);
    eurovisionRemoveState(eurovision, 9);
    CHECK(eurovisionRemoveJudge(eurovision, 13), EUROVISION_JUDGE_NOT_EXIST);
    eurovisionDestroy(eurovision);
    return true;
}

bool testAddVote() {
    Eurovision eurovision = setupEurovision();
    setupEurovisionStates(eurovision);
    CHECK(eurovisionAddVote(eurovision, -1, 12), EUROVISION_INVALID_ID);
    CHECK(eurovisionAddVote(eurovision, 12, -1), EUROVISION_INVALID_ID);
    CHECK(eurovisionAddVote(eurovision, 100, 12), EUROVISION_STATE_NOT_EXIST);
    CHECK(eurovisionAddVote(eurovision, 12, 100), EUROVISION_STATE_NOT_EXIST);
    CHECK(eurovisionAddVote(eurovision, 12, 1), EUROVISION_SUCCESS);
    eurovisionDestroy(eurovision);
    return true;
}

bool testRemoveVote() {
    Eurovision eurovision = setupEurovision();
    setupEurovisionStates(eurovision);
    setupEurovisionVotes(eurovision);
    CHECK(eurovisionRemoveVote(eurovision, -1, 12), EUROVISION_INVALID_ID);
    CHECK(eurovisionRemoveVote(eurovision, 12, -1), EUROVISION_INVALID_ID);
    CHECK(eurovisionRemoveVote(eurovision, 100, 12), EUROVISION_STATE_NOT_EXIST);
    CHECK(eurovisionRemoveVote(eurovision, 12, 100), EUROVISION_STATE_NOT_EXIST);
    CHECK(eurovisionRemoveVote(eurovision, 1, 12), EUROVISION_SUCCESS);
    CHECK(eurovisionRemoveVote(eurovision, 3, 4), EUROVISION_SUCCESS);
    CHECK(eurovisionRemoveVote(eurovision, 3, 7), EUROVISION_SUCCESS);
    CHECK(eurovisionRemoveVote(eurovision, 8, 3), EUROVISION_SUCCESS);
    CHECK(eurovisionRemoveVote(eurovision, 10, 11), EUROVISION_SUCCESS);
    eurovisionDestroy(eurovision);
    return true;
}

/** combined scores:
 * 4:  18 * 0.6 + 26 * 0.4 = 21.2 (moldova)
 * 3:  14 * 0.6 + 28 * 0.4 = 19.6 (russia)
 * 5:  15 * 0.6 + 20 * 0.4 = 17.0 (cyprus)
 * 10: 20 * 0.6 + 12 * 0.4 = 16.8 (united kingdom)
 * 6:  12 * 0.6 + 23 * 0.4 = 16.4 (spain)
 * 2:   9 * 0.6 + 20 * 0.4 = 13.4 (croatia)
 * 1:  10 * 0.6 + 12 * 0.4 = 10.8 (malta)
 * 7:   6 * 0.6 + 15 * 0.4 = 9.6 (italy)
 * 0:  14 * 0.6 +  0 * 0.4 = 8.4 (israel)
 * 14: 12 * 0.6 +  2 * 0.4 = 8.0 (netherlands)
 * 13: 12 * 0.6 +  1 * 0.4 = 7.6 (ireland)
 * 8:   4 * 0.6 + 10 * 0.4 = 6.4 (france)
 * 11: 10 * 0.6 +  1 * 0.4 = 6.4 (armenia)
 * 12:  8 * 0.6 +  0 * 0.4 = 4.8 (austria)
 * 9:   2 * 0.6 +  6 * 0.4 = 3.6 (germany)
 * 15:  3 * 0.6 +  0 * 0.4 = 1.8 (sweden)
 */
bool testRunContest() {
    Eurovision eurovision = setupEurovision();
    setupEurovisionStates(eurovision);
    setupEurovisionJudges(eurovision);
    setupEurovisionVotes2(eurovision);

    List ranking = eurovisionRunContest(eurovision, 50);
//    LIST_FOREACH(char*, str, ranking){
//        printf("%s, ",str);
//    }
//    printf("\n");
    CHECK(listGetSize(ranking), 43);
    char *current = (char*)listGetFirst(ranking);
    CHECK(strcmp(current, "israel"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "cyprus"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "austria"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "germany"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "italy"), 0);

    listDestroy(ranking);
    eurovisionDestroy(eurovision);
    return true;
}

bool testRunAudienceFavorite() {
    Eurovision eurovision = setupEurovision();
    setupEurovisionStates(eurovision);
    setupEurovisionJudges(eurovision);
    setupEurovisionVotes2(eurovision);

    List ranking = eurovisionRunAudienceFavorite(eurovision);
    CHECK(listGetSize(ranking), 43);
    char *current = (char*)listGetFirst(ranking);
    CHECK(strcmp(current, "israel"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "cyprus"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "italy"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "czech republic"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "denmark"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "germany"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "ukraine"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "moldova"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "estonia"), 0);
    current = (char*)listGetNext(ranking);
    CHECK(strcmp(current, "lithuania"), 0);

    listDestroy(ranking);
    eurovisionDestroy(eurovision);
    return true;
}

/* friendlies: malta-croatia, russia-moldova */
bool testRunGetFriendlyStates() {
    Eurovision eurovision = setupEurovision();
    setupEurovisionStates(eurovision);
    /* setupEurovisionJudges(eurovision); */
    setupEurovisionVotes2(eurovision);

    List friendlies = eurovisionRunGetFriendlyStates(eurovision);
    CHECK((friendlies == NULL), false);
    CHECK(listGetSize(friendlies), 3);
    char *current = (char *)listGetFirst(friendlies);
    CHECK(strcmp(current, "albania - italy"), 0);
    current  = (char*)listGetNext(friendlies);
    CHECK(strcmp(current, "bulgaria - cyprus"), 0);
    current  = (char*)listGetNext(friendlies);
    CHECK(strcmp(current, "estonia - lithuania"), 0);

    listDestroy(friendlies);
    eurovisionDestroy(eurovision);
    return true;
}
